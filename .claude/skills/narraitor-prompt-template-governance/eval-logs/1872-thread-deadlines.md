# Prompt/template eval log - world clock threads come due (#1872)

- Change: prompt-side, behind the shipped `WORLD_CLOCK` flag. Scene block: one thread becomes DUE NOW once it is overdue by three turns and the segment must land it, cutting time forward if the scene cannot reach it; open threads are never re-announced as news. Extraction: an advance must name a state change (`changed`), restatement is not an advance, `dropped` is not for stalled threads; the turn scale applies to every `dueByTurn`, and the store floors a due at two turns out.
- Diff: PR #1878 (`a4a40d01` on develop `068bde91`). Template files: `templates/narrative/worldClockBlock.ts`. Extraction: `src/lib/ai/worldThreadExtraction.ts`, `goalExtractor.ts` (skeleton field `changed`). Arithmetic: `src/lib/narrative/worldClock.ts` (`selectDueNowThread`, `overdueByTurns`, `DUE_NOW_OVERDUE_TURNS = 3`, `MIN_DUE_HORIZON_TURNS = 2`). Store: `worldThreadStore.applyExtraction` (due floor, `changed` into notes). No orchestrator, controller, flag or lore-extractor change.
- Date / evaluator: 2026-08-18, Claude (build session), judged blind per `narraitor-playtest-loop`.

## Declared matrix (written before the code)

Same two cells as round 5 (#1818), same Cautious autopilot (lowest DC, then lawful, offered options only, no typed input), same judge prompts verbatim, live Gemini, this worktree's server.

| Cell | Arms | Judge | Comparator (round 5) |
|---|---|---|---|
| Harrowgate Mills (civic drama), Wren Calloway fresh, 30 turns | this build, flag on (default) | blind, blocks 1-10 / 11-20 / 21-30 plus the "World moved on its own" line | treatment: Momentum 3/4/2, world-moved 4/4/4, ledger 15 advances / 0 resolved / 2 dropped / 4 overdue at t30; control 3/2/3 |
| Camp Crystal Lake (slasher), Jamie Holt established: 15 turns flag off, server restarted flag on, same session to t45 | this build both phases | blind, blocks 1-15 / 16-30 / 31-45 plus the five planted facts | Momentum 2/4/1, world-moved 3/10/12 (11 re-announced), ledger 18 advances / 0 resolved / 1 dropped |

Ledger metrics per session, computed by a script over the posted artifact and never shown to a judge: resolved (outcome `resolved`), dropped, advances, longest overdue streak per thread, threads with `dueByTurn <= openedAtTurn + 1` (must be 0 after the floor), the advance notes for a restatement read.

Gates:
- G-A: resolved-not-dropped >= 1 per session.
- G-B: last-block Momentum >= first treated block (Harrowgate block 3 vs block 1; Crystal Lake 31-45 vs 16-30).
- G-C (soft, N=1): Memory / Voice / Choice not more than 1 below round 5's same block; any new failure mode named.
- G-D: zero new AI calls per turn (same `/api/narrative/*` call set).
- G-E: quality gate and CI green.

Decision rule: G-A and G-B pass in both cells -> SHIP (merge, flag stays on). Either fails in both cells -> HOLD (PR stays draft, memo names the gate, the round-5 clock stays as shipped). Split -> HOLD naming the cell.

## Round 7, declared before the code (2026-08-18)

The memo's three re-entry changes on the same PR, wording only, no seam or store change:
1. OPEN: an offstage threat the prose introduces (a sound, an arrival, a message, a move by someone not in the scene) is a thread, and a major event the player did not cause with no open thread covering it gets opened; every summary is phrased as the event that will land, not a standing state ("Thorne comes to collect the favor" rather than "the player owes a favor"). The seed phrases its threads the same way. The block's empty-ledger move is named as a thread the story now owes.
2. RESOLVE: `resolved` requires `resolution` to name the outcome (who won the vote, what came through the door, what was lost); the calling, the departure toward it, or a change of sound is an advance.
3. DUE NOW keeps its pick rule; with every thread event-shaped by construction there is nothing state-shaped to exclude, and the section's landing list gains "the debt is collected".

Same two cells, same autopilot, same judge prompts, same gates G-A to G-E and the same decision rule as round 6. Comparators: round 6 (this PR before the changes) first, round 5 as context.

## Gates

- G1 input contract: `WorldClockPromptContext.threads[i]` gains `overdueByTurns` and `dueNow`, both computed in `buildWorldClockPromptContext` from the store's own turn indices; `WorldThreadExtractionResult.advanced[i].changed` replaces the optional `note`. Nothing smuggled through `currentSituation`; the seam (`NarrativeContext.worldClock`) is unchanged.
- G2 leakage: the block adds only the picked thread's own summary and its overdue count. No ids, no store internals. The extraction section adds a turn scale and rule wording; the seed still reads `toneSettings.customInstructions` as before.
- G3 determinism: scene response JSON unchanged. Extraction JSON's `advanced` entries change field name (`changed`); the parser drops entries without it (fail-open to "no advance", unit-tested), everything else parses as before.
- G6 cost, measured in chars (~4 per token), no new call: scene block 821 empty / 1,203 at 3 threads / 1,981 at 3 threads with a DUE NOW pick / 2,471 at the 8-thread cap with a pick (round 5: 664 / 1,062 / 1,881). Extraction section 1,559 / 1,931 / 2,571 at 0 / 3 / 8 threads (round 5: 1,805 / 2,629 / 2,427 on the seed turn). The DUE NOW section is about 780 chars (~195 tokens) and renders only while a pick exists.
- G7 integration: live loop through `/worlds/[id]/play` on this worktree, both flag states (Crystal Lake phase 1 flag off, phase 2 flag on); the same `/api/narrative/*` call set as round 5 (`generate`, `choices`, `summarize`, `validate-event-significance`), no new endpoint.

## Coverage matrix

| World (genre/tone) | Character (fresh/established) | Runs | Verdict | Representative excerpt (1-3 lines) |
|---|---|---|---|---|
| Harrowgate Mills (civic drama, dramatic) | fresh, Wren Calloway | 30 turns, this build flag on, Cautious autopilot (29 picks, all DC 0, 0 errors), one blind judge on `rubric.md` | mixed. Judge: Momentum 3/2/3, Stakes 3/3/3, Surprise 3/4/3, Agency 3/2/3, Memory 3/2/2, Voice 3/2/2, Choice 3/3/3; world-moved 3/3/4. Round 5 treatment: 3/4/2, 3/3/2, 4/3/2, 3/3/3, 4/2/2, 3/2/2, 3/3/2; 4/4/4. Ledger: 3 seeded t1 + 1 opened t25, 23 advances, 1 resolved, 0 dropped, 2 overdue at the last stamped turn (t29), 0 floor breaches, 28/30 stamps (t12, t30 missing). The one resolution is the six-week vote, recorded at t29 as "The vote has been formally called by Martha Vance"; the judge's sharpest problem is that "the vote is called" lands at t10, t14, t15, t17, t18, t19, t29 "and never once resolves". The DUE NOW pick from t6 to t30 was the state-shaped seed thread "player's debt to those who appointed them" (due 3, floored from the seed); it sat DUE NOW for 24 turns, was hand-waved at t24 ("the pressure from your appointed supporters ... now dissipates") and never resolved. Zero `transition` segments: the forward time cut was never taken. | t25: "an independent assessment of Rowan Textiles' value will be commissioned ... the doors at the back of the room creaked open, admitting two stern-faced individuals in dark suits". t29: "Martha Vance ... brings her gavel down with a sharp, final crack. 'The vote is called'". Judge, block 3: "the one time the player wins a real concession (T25's independent assessment) it is overwritten by the same line four turns later." |
| Camp Crystal Lake (slasher, mysterious, R) | established, Jamie Holt (15 turns flag off, server restarted flag on, same session to t45) | single arm; blind judge in three 15-turn blocks; autopilot 14 + 30 picks, all DC 0, 0 errors | ledger improved, story did not. Judge: Momentum 2/3/2, Stakes 2/3/4, Surprise 3/3/3, Agency 3/4/2, Memory 3/2/1, Voice 3/2/1, Choice 2/2/1; world-moved 4/12/13. Round 5: 2/4/1, 2/3/2, 2/3/1, 3/4/2, 3/2/2, 2/2/1, 2/2/1; 3/10/12. Seed at t16 returned zero threads again (#1873). Two threads opened (t19 whistle from the lodge, due 21; t27 thwack from the shed, due 29), 5 advances, both resolved (t22: the pair reach the lodge, the dog is dead; t36: the thwack "replaced by a new, more descriptive and disturbing sound"), 0 dropped, 0 open at the end; the thwack thread was DUE NOW t32-t35 and landed at t35-36 (a plank bursts inward, the sound changes) after 7 overdue turns against round 5's 25. Ledger empty for t37-t45 (nothing opened in nine turns of a siege) while the judge counts seven concurrent offstage menaces. 28/30 treated stamps (t37, t39 missing), 0 floor breaches, zero `transition` segments. Five planted facts at t45: radio held; town distance, local knowledge and 1984 forgotten; "no kids" contradicted (campers and a lost child from t36). | t22 (whistle thread lands): the lodge in disarray, the dog's collar torn and bloody. Judge, block 3: "43, 44, and 45 each have the player's back hitting a cold wall that 'isn't the one you expected' ... Turn 45 is turn 44 with one new CRACK." Sharpest problem: "Every turn from 16 onward introduces a new offstage threat sound ... and none of them ever arrives, is seen, or resolves". |

Matrix shortfall, stated: N=1 per cell, one judge each, two of the protocol's four cells, as declared. Round 5's numbers are the comparator; no same-day control arm was run.

## Arc check (>= 3 consecutive turns, one cell)
- Harrowgate: the six-week vote (due t30) is pulled into the single council meeting from t7 and stays in a called / delayed loop for the rest of the session (called at t10, t14, t15, t17, t18, t19, t29; delayed by the player's procedural options at t11, t20, t23, t24, t30). The extractor recorded 18 advances on it, most of them real state changes by the new rule's letter ("hands raised", "delayed", "called again"), and one `resolved` at t29 on the calling, not the outcome. The judge reads it as never resolving. Two verbatim re-narrations (t16 mirrors t13, t24 mirrors t23) and a genre-placeholder location at t18 and t28 (#1871).
- Crystal Lake: the whistle thread is the round's one clean payoff (t19 open, t22 the dog dead in the lodge, judge: "the first real loss"). After t36 the ledger is empty and the block's empty-ledger rule ("MUST show the world moving on its own") produces a new offstage sound almost every turn (world-moved 13/15 in block 3) that the extractor never files as a thread, so nothing is owed and nothing lands; the judge's block-3 Memory 1 / Voice 1 / Choice 1 is the same collapse round 5 saw.
- Contradictions found: Crystal Lake t36-t45 campers and a lost child against "no kids yet"; an axe conjured for both characters (t37, t39); a door both braced (t30-31) and gone (t35). Harrowgate t30 counts three suits where t25-26 established two.

## Failure drill
- Malformed/empty response path: `parseWorldThreadExtraction` drops advances without `changed` (unit-tested alongside the existing junk cases); an all-restatement turn therefore reads as "nothing moved", which is the intended failure direction. Fail-open elsewhere unchanged: 2 of 30 stamps missing in each cell (Harrowgate t12, t30; Crystal Lake t37, t39), the prompt read one turn stale, no player-visible effect.
- Slow-response/timeout behavior: unchanged, extraction is fire-and-forget after `addSegment`.
- Harness, not product: a hidden browser pane throttles the autopilot's `setTimeout` to once a minute after ~5 min (Harrowgate turns went 6 s -> 35 s -> 60 s while the server answered in ~2 s); a network-backed sleep against the port-8400 receiver fixed it (Crystal Lake mean 6.7 s per turn). Recorded in the harness memory.

## Regression vs prior good outputs
- Compared against: round 5's same cells (`fd475d46`, flag on) as the comparator; round 5's flag-off control as context.
- Old strengths preserved? Harrowgate: world-moved 3/3/4 vs 4/4/4 (flat within N=1), Memory 3/2/2 vs 4/2/2 (block 1 down one), Voice equal, Choice 3/3/3 vs 3/3/2. Crystal Lake: world-moved 12/13 vs 10/12 in the treated blocks, Memory 2/1 vs 2/2 (block 3 down one), Voice 2/1 vs 2/1, Choice 2/1 vs 2/1. Ledger metrics all moved the right way in both cells: resolved 0 -> 1 and 0 -> 2, dropped 2 -> 0 and 1 -> 0, longest overdue streak 27 (a state-shaped thread) and 25 -> 7, floor breaches 4 -> 0 and n/a -> 0. Failure handling unchanged (every autopilot pick DC 0 in both cells; the failure template is #1821's territory).
- New failure modes named: (1) the "vote is called" loop, where a deadline pulled forward is called and blocked turn after turn and the extractor accepts the calling as the resolution; (2) DUE NOW on a state-shaped thread ("owes a debt") produces a hand-wave, not a landing, and never clears; (3) with an empty ledger the block's fallback manufactures a new offstage sound each turn that the extractor never files, so the story menaces without owing.
- Not the clock's: Crystal Lake t26 ends "This narrative segment was generated by an AI assistant." and t30 ships a raw metadata block as prose (the #1859 / #1870 family); "Abandoned Mansion" location placeholder t23-t29 (#1871).

## Cost/latency
- Token delta: see G6. About 195 extra input tokens per turn while a DUE NOW pick exists; the block without a pick is roughly 35 tokens larger than round 5's at 3 threads. Zero new calls (G7).
- Latency: none added on the turn path. Harrowgate mean 9.3 s per turn is the harness throttling, not the model (server-side `generate` 0.5-6 s in the logs); Crystal Lake phase 2 mean 6.7 s.

## Verdict
- HOLD, by the rule declared above. G-A passes on the count in both cells (Harrowgate 1 resolved / 0 dropped, Crystal Lake 2 / 0), with the caveat that Harrowgate's one resolution is the calling of a vote the judge says never happens. G-B passes in Harrowgate (block 3 Momentum 3 vs block 1 3; up from round 5's 2) and fails in Crystal Lake (31-45 Momentum 2 vs 16-30 3; round 5 was 1 vs 4). Split -> HOLD naming Crystal Lake. G-C within one point everywhere, G-D and G-E pass.
- What the round shows: the ledger side of #1872 moved (resolutions above zero, no drops, overdue streaks a third of round 5's, no near dues), the DUE NOW pick landed an event-shaped thread inside four turns, and the last block is one point better than round 5 in both cells. What it did not show: last-block Momentum recovering to the first treated block in the slasher cell, and the prose-side symptom the issue names ("re-announces the same event instead of paying it off") is still the sharpest problem both judges found, now as a vote called seven times and seven offstage sounds that never arrive.
- Ship/hold decision recorded at: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1872-thread-deadlines.md` (HOLD; re-entry condition there). Round posted to #1818 as round 6.

## Round 7 results (2026-08-18, build `f213e706`)

The three wording changes declared above, nothing else: OPEN files offstage threats and uncovered major events as event-shaped threads and the seed phrases its threads the same way; RESOLVE requires `resolution` to name the outcome; the DUE NOW landing list gains "the debt is collected" and the empty-ledger move is "a thread the story now owes". Sizes (chars): block 965 empty / 1,203 at 3 threads / 2,091 with a pick / 2,581 at 8 with a pick (round 6: 821 / 1,203 / 1,981 / 2,471); extraction section 2,212 / 2,584 / 3,224 at 0 / 3 / 8 threads (round 6: 1,559 / 1,931 / 2,571). Same call set, zero new calls. Quality gate: jest 426 suites / 2,969 tests exit 0, tsc 0, eslint 0; CI green on `f213e706`.

### Coverage matrix, round 7

| World (genre/tone) | Character (fresh/established) | Runs | Verdict | Representative excerpt (1-3 lines) |
|---|---|---|---|---|
| Harrowgate Mills (civic drama, dramatic) | fresh, Wren Calloway | 30 turns, flag on, Cautious autopilot (29 picks, all DC 0, 0 errors, mean 7.1 s a turn), one blind judge on `rubric.md`, round-5 prompt verbatim | FAIL on G-B. Judge: Momentum 3/3/1, Stakes 3/4/3, Surprise 3/4/4, Agency 3/3/2, Memory 3/2/1, Voice 2/2/1, Choice 2/2/2; world-moved 5/7/9. Round 6: 3/2/3, 3/3/3, 3/4/3, 3/2/3, 3/2/2, 3/2/2, 3/3/3; 3/3/4. Ledger (script): 9 threads (3 seeded t1 + 6 opened t9, t12, t17, t18, t21, t28), 23 advances, 2 resolved at t8 with named outcomes (the vote: "voted to accept The Abacus Group's offer"; the offer: "accepted by the council"), 0 dropped, 7 open / 6 overdue at t30, longest overdue 27 turns on the seed thread "those to whom the player owes favors will seek repayment" (never advanced), 30/30 stamps, 0 floor breaches, 0 `transition` segments. DUE NOW pick recomputed from the ledger with `selectDueNowThread`: the vote thread t6-t8 (landed t8, real outcome, 3 turns), then the favors thread t9-t30 (22 turns) while Henderson (t9), Sarah (t18) and Abernathy (t21) arrived to collect and were each filed as a NEW thread instead of advancing or resolving the one already DUE NOW. Metadata in the prose t13, t20 (img tags), t24, t29. | t8: "The council has voted to accept The Abacus Group's offer for Harrowgate Mills". Judge, block 3: "Turn 27 pastes the county-official arrival paragraph from turn 25 verbatim, twice in one passage"; sharpest problem: "Henderson's 'the bank called, they want the roof money today' is delivered as news at turns 9, 15, 19, 24, and 29 ... Not one of these ever comes due". |
| Camp Crystal Lake (slasher, mysterious, R) | established, Jamie Holt (15 turns flag off, server restarted flag on, same session) | 24 play turns + epilogue: the session ended fatally at t24 (Jamie killed, "The End"), so the declared 31-45 block was never reached and G-B is NOT SCORABLE for this cell. Autopilot 14 + 9 picks, all DC 0, 0 errors. Blind judge on the round-5 prompt with the deviation stated (blocks 1-15 / 16-24 plus the epilogue, plus "was the ending earned") | not scorable on the declared gate; the mechanism read is informative. Judge: Momentum 2/3, Stakes 2/4, Surprise 2/3, Agency 2/2, Memory 3/2, Voice 2/2, Choice 2/2; world-moved 4 / 8 of 9. Round 6 first two blocks: 2/3, 2/3, 3/3, 3/4, 3/2, 3/2, 2/2; 4/12. Ledger (script, t16-24): seed at t16 returned ONE thread this time (actor, "whatever caused the clang at Cabin Seven will make its presence known", due 18; #1873's empty seed did not reproduce), one more opened t21, 6 advances, 0 resolved, 0 dropped, 2 open / 1 overdue at the last stamp (t23), 7/9 stamps (t19, t24 missing), 0 floor breaches, 0 `transition`. The seed thread went DUE NOW at t21 and the arrival happened at t21 (a man in a goalie mask, a second figure crashing out of Cabin Seven), t23 (a wet CRACK, the porch figure silenced), t24 (the masked man advances) and the epilogue (the machete); the extractor filed all of it as advances and never resolved the thread, so the ledger says 0 resolved for a thread the story paid off with the player's life. Judge on the ending: "half earned", the causal chain is real (the ankle at t18, the wide throw at t19) but the blow lands offscreen between t24 and the epilogue, and the epilogue is truncated mid-word. | t21: "not a person, but a towering man ... the goalie mask"; judge, block 2: "The world finally starts acting (Cabin Seven, the masked man, the second body), so momentum and stakes lift from 2 to 3 and 4. Everything else gets worse: two passages are outright duplicates (17/18, 21/22)". Sharpest problem: "The generator re-narrates the previous turn instead of continuing from it." |

Matrix shortfall, stated: N=1 per cell, one judge each; the Crystal Lake cell did not reach its declared last block, and a second attempt was not run because Harrowgate already fails G-B, so the verdict is HOLD whichever way a 45-turn Crystal Lake read came out (recorded in the memo as a decision, not a finding).

### Arc check (>= 3 consecutive turns, one cell), round 7
- Harrowgate: the vote thread landed at t8 with a real outcome under DUE NOW (called t6, hands raised t7, "the motion carries" t8), the first clean resolution the deadline mechanism has produced in this cell. From t9 the DUE NOW pick was the seed's favors thread, and the favors did come to collect: Henderson t9 (roof loan), Sarah t18 (a debt), Abernathy t21 (garden loan). The extractor, told to file every arrival as a thread, opened a new actor thread for each instead of advancing or resolving the DUE NOW one, so the block kept asking for the favors to land after they had, and each new thread then re-fired as news (Henderson t9/15/19/24/29, the county man t17/25/27 verbatim, Abernathy's envelope t21/26). One of the six opened threads was already past when filed (t17 "has arrived and delivered critical news") and sat overdue for eleven turns as something still owed. Momentum 1 in block 3 is the judge reading the loop.
- Crystal Lake: the seed's clang thread is the round's clean payoff and the cell's ending: filed t16 due 18, DUE NOW t21, the masked man at t21, the kill by the epilogue. What the ledger cannot say is that it paid off: the RESOLVE rule asks for "what came through the door", and the extractor wrote that as `changed` on four advances ("the cabin figure's true form was revealed as a large man in a goalie mask") without ever using `resolved`. The re-narration the judge names (t22 = t21, t18 = t17, t24 = t23) is #1868's family, not the clock's, and it ate the block's Momentum 4.
- Contradictions found: Crystal Lake t19 log "out of reach" vs t21 bracing it; `Abandoned Mansion` in the location field at t16 and t21 (#1871) and Harrowgate t11, t13, t20, t23, t24, t29 (judge). Harrowgate t20 "the motion carries" a second time after t8, and the judge counts the rezoning arriving as new three times.

### Regression vs round 6 (same PR before the wording), round 7
- Harrowgate: last-block Momentum 3 -> 1, Memory 2 -> 1, Voice 2 -> 1, Choice 3 -> 2 in every block; Stakes and Surprise up in blocks 2-3 (4/3, 4/4). World-moved 3/3/4 -> 5/7/9: the world moves far more and the judge likes it less, because the moves are the same three arrivals re-fired. G-C holds by its letter: block-3 Memory 1 and Voice 1 are exactly one below round 5's 2 / 2, Choice 2 is one below round 5's 3 in block 1, nothing is more than one below. Ledger: resolved 1 -> 2 and both are outcomes now (the round-6 "vote formally called" would fail the new RESOLVE test and did not recur), dropped 0 -> 0, threads 4 -> 9, open at end 3 -> 7, overdue 2 -> 6, longest overdue 27 -> 27 (the same seed thread, unadvanced both rounds), stamps 28/30 -> 30/30, floor breaches 0 -> 0, transitions 0 -> 0.
- Crystal Lake: only blocks 1-15 / 16-24 exist. Against round 6's first two blocks, treated-block Momentum 3 -> 3, Stakes 3 -> 4, Agency 4 -> 2, Memory 2 -> 2, Voice 2 -> 2, Choice 2 -> 2. Seed non-empty (0 -> 1 thread at t16). Resolved 2 -> 0 (round 6's two resolutions were the whistle and the thwack; this round's one landing was written as advances). Nothing to say about the last block.
- New failure modes named: (1) reconciliation gap: an arrival that IS the DUE NOW thread landing gets filed as a new thread, so the pick never clears and the ledger multiplies (9 threads, 6 overdue at t30); (2) filed threads re-fire as news anyway ("never introduce one as new" did not hold once the ledger held six overdue actors); (3) the stricter RESOLVE made the extractor withhold `resolved` on a real payoff (Crystal Lake's masked man). Fixed from round 6: resolutions name outcomes; the state-shaped DUE NOW hand-wave did not recur because the pick landed the vote first.
- Not the clock's: raw metadata / img tags / a JSON block reaching the prose (Harrowgate t13, t20, t24, t29; Crystal Lake t16 "Goals Achieved:" list, t21 metadata block), the #1859 / #1870 family; `Abandoned Mansion` (#1871); the truncated epilogue ("cursed ground w"); verbatim re-narration after a hold/maintain pick (#1868 family).

### Verdict, round 7
- HOLD, by the declared rule. G-A passes in Harrowgate (2 resolved, both named outcomes, 0 dropped) and fails in Crystal Lake on the count (0 resolved) even though the thread landed. G-B FAILS in Harrowgate (block 3 Momentum 1 vs block 1 3) and is not scorable in Crystal Lake. G-C holds by its letter (nothing more than one below round 5's block), G-D and G-E pass. G-B fails in one cell and cannot be scored in the other, so the split branch applies and names Harrowgate. This is a HOLD that is worse than round 6's on the story read and better on the ledger's honesty (resolutions are real).
- What the round shows: DUE NOW lands event-shaped threads fast when there is one to land (the vote in 3 turns with an outcome, the masked man in 1 turn); "file every offstage threat" is the wrong lever, it multiplied the ledger to nine threads, most of them the same favor coming due under different names, and the block's re-announcement rule cannot hold six overdue actors quiet. Wording rounds 6 and 7 both moved the ledger and neither moved the last block; the missing piece is structural, not another sentence.
- Ship/hold decision and the re-entry condition: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1872-thread-deadlines.md`. Round posted to #1818 as round 7.

## Round 8, declared before the code (2026-08-18): reconciliation on OPEN

The memo's re-entry item 1, and only that. Structural, not wording: the extraction contract and the store change; the scene block, `selectDueNowThread`, the seam and the flag do not.

1. Extraction contract: every `opened` entry may carry `covers`, the id of an open thread that this arrival, message, sound or move is landing or moving; null (or absent) when nothing open covers it. The JSON skeleton shows the field. The parser keeps it as-is (fail-open: a missing or unknown value opens as before).
2. Store routing: an opened entry whose `covers` names one of this session's open threads does not create a thread. It refines that thread: the summary becomes the specific event the model just named ("Mr. Henderson comes to collect the roof loan", not "those who appointed you will seek repayment"), the kind follows the entry, `dueByTurn` is re-filed from the entry when given (floored at the horizon as before) and kept otherwise, `lastAdvancedAtTurn` moves, and the old summary is kept in `notes` as `<new> (was: <old>)`. It counts as an advance in the segment stamp. A `covers` that points at a resolved or foreign thread opens a new thread, as today.
3. Extraction prompt: the thread list marks each overdue thread with "OVERDUE by N turns" and the one the scene block asked to land with "DUE NOW: this segment was asked to land it", computed by `applyWorldClockUpdates` from the same store state and turn index the block used (`dueNowThreadId` on the extraction input). The OPEN rule says to check the list first, the DUE NOW thread first, and to file a match under `covers` instead of as new.

Why this and not more: round 7's nine threads were the seed's favors thread landing three times under three names; with the specific event replacing the vague one and its due re-filed, the DUE NOW pick clears by arithmetic when the model gives the arrival a timescale, and the ledger stays at the number of pressures the story actually has. Item 2 (a fired state rendered to the scene block as "already in the room") is NOT in this round; if re-announcement survives reconciliation it is the next round.

Matrix: the same two cells, same Cautious autopilot, same judge prompts (round-5 verbatim; if Crystal Lake ends before its last block, the shortened-transcript prompt from round 7 with the deviation stated AND a second attempt in the same round), same gates G-A to G-E, same decision rule. Comparators: round 7 first (same PR, wording only), round 6 and round 5 as context. New ledger metric: reconciliations per session (opened entries routed into an existing thread, read from the `(was:` notes), and threads open at the end; a pass on the story gates with reconciliations at zero means the mechanism did nothing and the read is noise. Sizes measured after the code, recorded in "Round 8 results".

## Round 8 results (2026-08-18, build `7af4d573`)

Reconciliation on OPEN as declared: `covers` on opened entries, store refines the covered thread (summary, kind, re-filed due, note `<new> (was: <old>)`), extractor thread list marks OVERDUE by N and DUE NOW from `dueNowThreadId`. Scene block, `selectDueNowThread`, seam and flag unchanged. Sizes (chars): block unchanged at 965 / 1,203 / 2,091 / 2,581; extraction section 2,723 / 3,095 / 3,735 at 0 / 3 / 8 threads (round 7: 2,212 / 2,584 / 3,224), 3,171 at 3 threads with a DUE NOW mark; about +130 tokens for the covers rule. Same call set, zero new calls. Quality gate: jest 426 suites / 2,973 tests exit 0, tsc 0, eslint 0.

### Coverage matrix, round 8

| World (genre/tone) | Character (fresh/established) | Runs | Verdict | Representative excerpt (1-3 lines) |
|---|---|---|---|---|
| Harrowgate Mills (civic drama, dramatic) | fresh, Wren Calloway | 30 turns, flag on, Cautious autopilot (29 picks, all DC 0, 0 errors, mean 7.1 s), one blind judge, round-5 prompt verbatim | FAIL on G-B. Judge: Momentum 3/3/1, Stakes 3/3/2, Surprise 3/3/1, Agency 2/3/2, Memory 2/2/2, Voice 2/2/2, Choice 2/2/2; world-moved 4/5/0. Round 7: 3/3/1, 3/4/3, 3/4/4, 3/3/2, 3/2/1, 2/2/1, 2/2/2; 5/7/9. Ledger (script): 4 threads (2 seeded t1 + t2 + t6), 19 advances of which 1 reconciliation, 3 resolved with named outcomes (t2 the vote "carried"; t14 the agreement "signed by Martha Vance"; t29 Henderson's "ledger officially submitted as evidence for review"), 1 dropped (t15 Albright, "initial arrival was covered in a previous segment"), 0 open / 0 overdue at t30, longest overdue 14, 29/30 stamps (t7 missing), 0 floor breaches, 0 `transition`. Reconciliation: at t12, when Henderson stepped forward from the back of the room, the seed's "those who appointed the player will come to collect on the favor owed" (due 3, DUE NOW since t6) became "Mr. Henderson arrives to collect on a favor owed by Wren Calloway, presenting a worn ledger" (consequence, due re-filed to 15). DUE NOW pick recomputed from the stamps: the favors thread t6-t12 (landed as Henderson at t12), the signed-agreement deadline t13-t14 (signed t14), Albright t15 (dropped), Henderson t18-t29 (landed t29). Nothing opened after t6 while the judge counts arrivals at t7 (Sarah), t9 (Thorne), t11-13 (two unnamed), t15 (a second county official with the same report). | t3: the favors thread reconciled into Henderson's ledger. Judge, block 3: "The ledger slides across the table to the player at 25, then again at 28. Thorne answers ... at 27 and again at 30 with the same answer"; "World moved on its own: 0". Sharpest problem: "Failure has one shape and it is not the player's ... 'your words catch in your throat' regardless of whether the player demanded a review, apologized, asked a question, or challenged a signature." |
| Camp Crystal Lake (slasher, mysterious, R) | established, Jamie Holt (15 turns flag off, server restarted flag on, same session to t45) | full 45 turns this time (no early ending), autopilot 14 + 30 picks, all DC 0, 0 errors, mean 7.1 s; blind judge in three 15-turn blocks plus the five planted facts, round-5 prompt verbatim | PASS on G-B (first time in this cell). Judge: Momentum 2/3/3, Stakes 2/2/3, Surprise 2/3/3, Agency 3/3/3, Memory 3/2/1, Voice 2/2/2, Choice 2/3/2; world-moved 3/14/14. Round 6: 2/3/2, 2/3/4, 3/3/3, 3/4/2, 3/2/1, 3/2/1, 2/2/1; 4/12/13. Round 5: 2/4/1, 2/3/2, 2/3/1, 3/4/2, 3/2/2, 2/2/1, 2/2/1; 3/10/12. Ledger (script, t16-45): seed at t16 one thread (a dog barking from Cabin Seven, due 18; #1873 did not reproduce), t22 "an unknown entity or person is operating a light at the boathouse" (due 24), t42 a third figure in the boathouse doorway (due 44); 17 advances, 1 resolved (t19, "the barking has ceased, replaced by a chilling quiet", a change of sound the RESOLVE rule calls an advance), 0 dropped, 2 open / 1 overdue at the last stamp (t43), longest overdue 21 (the boathouse light, DUE NOW t27-t45 and never resolved while its 16 advances name the woman, the skiff, the machete, the boat grounding, the beam finding them), 22/30 stamps (missing t21, 23, 30, 31, 33, 40, 44, 45), 0 floor breaches, 0 `transition`, 0 reconciliations. Five facts at t45: town and radio forgotten, local knowledge partly held, no kids held by t1 only, 1984 not contradicted; night downgraded to "dusk" at t43 and t45. | t32: the hulking figure of t29-31 revealed as "a gaunt woman in a wet dress" with a machete, calling for Jason. Judge, block 3: "Escalation continues: the woman with the machete (32), the boat grounding (34-35), the beam finding them (36), a hulking man rising from the skiff (39) ... the gurney (45)"; sharpest problem: "The game re-emits its own earlier passages inside new ones ... turn 33 replays turns 29-33 in order before adding one new paragraph." |

Matrix shortfall, stated: N=1 per cell, one judge each, both cells reached their declared last block; round 5 remains the comparator with no same-day control arm. Eight of thirty Crystal Lake extraction stamps are missing (rounds 5-7: 0-2 per cell); the extraction path is fail-open with no logging, so whether the model's answer failed to parse (the `covers` field, a longer skeleton) or the call failed cannot be told after the fact. Recorded as a regression signal, not a finding.

### Arc check (>= 3 consecutive turns, one cell), round 8
- Harrowgate: the mechanism did the thing it was built for once and cleanly. Henderson arrived at t12, on the favors thread's seventh DUE NOW turn, and the extractor filed him under it (`covers`) instead of beside it; the ledger never went above three open threads; nothing was re-fired as news (neither judge names re-announcement as the sharpest problem this round, for the first time since round 5). Then it fell into the loop the memo predicted for item 2: from t18 the only open thread was Henderson's ledger, DUE NOW for twelve turns, its summary still saying "arrives to collect" while he had stepped forward with the ledger at t12 and it lay on the table from t21; the block asked every segment to land an arrival that had happened, the extractor recorded each restatement as an advance ("silent plea indicates his expectation", "hopes are fading"), and the judge scores block 3 Momentum 1, world-moved 0. Nothing was opened for 24 turns even as officials kept walking in, so the empty-ledger fallback had nothing to work with when the thread finally resolved at t29.
- Crystal Lake: the block's payoff run is real: boathouse light t22, on the dock t27, "Jason" t28, the boat t29, the woman with the machete t32, grounding t34-35, the beam finds them t36, the hulking man t39, the third figure t41, the gurney t45. Momentum 3 held through the last block, the first time this cell has not collapsed. The ledger side is the reverse of Harrowgate: one thread carried the whole siege for 19 DUE NOW turns and never resolved because the extractor kept naming each step as `changed` and never wrote `resolved` for the woman reaching the bank (the RESOLVE rule's "what came through the door" happened at t34-36 and was filed as an advance). Judge's contradictions: the antagonist's sex and build (hulking man t29-31, gaunt woman t32, hulking man again t39), night to dusk at t43/45, `Abandoned Mansion` at t24, 27, 32, 33, 39 (#1871). The passage re-emission the judge names (t10 = t9, t24 = t21+t23+t21, t33 replays t29-33, t39 stacks five recaps) is #1868's family and is the sharpest problem in this cell as it was in round 7.
- Contradictions found: Harrowgate t7 "Abandoned Mansion" and a duplicated passage inside stray HTML head/body markup; t17 a leaked "Player's Journal" block; t19 raw metadata fields as prose; t15 a second official carrying the report Thorne already brought at t9. Crystal Lake t21 Mark at the fire pit fifteen turns after leaving it.

### Regression vs round 7 (same PR, wording only), round 8
- Harrowgate: last-block Momentum 1 -> 1, Stakes 3 -> 2, Surprise 4 -> 1, Agency 2 -> 2, Memory 1 -> 2, Voice 1 -> 2, Choice 2 -> 2; world-moved 5/7/9 -> 4/5/0. Ledger: threads 9 -> 4, resolved 2 -> 3 (all named outcomes both rounds), dropped 0 -> 1, open at end 7 -> 0, overdue at end 6 -> 0, longest overdue 27 -> 14, reconciliations n/a -> 1, stamps 30/30 -> 29/30. G-C by its letter against round 5's same blocks: Memory 2 vs 2, Voice 2 vs 2, Choice 2 vs 2 in block 3, pass; block 1 Memory 2 vs round 5's 4 is two below, so G-C FAILS in block 1 on Memory (the judge cites the `Abandoned Mansion` field and a duplicated passage in HTML markup at t7, both #1871 / #1868 family, not the clock's).
- Crystal Lake: only round 5 and 6 have a comparable last block. Against round 6: Momentum 2 -> 3, Stakes 4 -> 3, Surprise 3 -> 3, Agency 2 -> 3, Memory 1 -> 1, Voice 1 -> 2, Choice 1 -> 2; world-moved 13 -> 14. Against round 5: 1 -> 3, 2 -> 3, 1 -> 3, 2 -> 3, 2 -> 1, 1 -> 2, 1 -> 2. G-C: Memory 1 is one below round 5's 2, pass. Ledger: threads 2 -> 3, resolved 2 -> 1, dropped 0 -> 0, open at end 0 -> 2, longest overdue 7 -> 21, stamps 28/30 -> 22/30, seed non-empty (0 -> 1).
- New failure modes named: (1) one-thread treadmill: with reconciliation holding the ledger small, a single DUE NOW thread whose event has already fired stays "arrives" for a dozen turns and the block funnels every segment into it (Harrowgate t18-29); this is the memo's item 2 in the data. (2) Under-opening: 0 opens in 24 Harrowgate turns with arrivals in the prose the judge counts at t7, 9, 11-13, 15; the covers rule appears to have swung the extractor from filing everything (round 7) to filing nothing, N=1. (3) Advances instead of a resolution on a thread that visibly landed (Crystal Lake t34-36), the round-7 mode again. (4) 8/30 stamps missing in one cell, unexplained.
- Fixed from round 7: threads re-fired as news is no longer either judge's sharpest problem; the ledger did not multiply (4 and 3 threads); the DUE NOW pick landed all four of its Harrowgate targets (the favors thread as Henderson's arrival, the agreement, Albright dropped, Henderson's ledger) and drove Crystal Lake's siege to a last block that held.
- Not the clock's: the failure template ("your words catch in your throat", #1821), passage re-emission (#1868), metadata / journal / HTML markup in the prose (#1859 / #1870 family), `Abandoned Mansion` (#1871).

### Verdict, round 8
- HOLD, by the declared rule, naming Harrowgate. G-A passes in both cells (Harrowgate 3 resolved / 1 dropped; Crystal Lake 1 / 0). G-B PASSES in Crystal Lake (31-45 Momentum 3 vs 16-30 3; rounds 5 and 6 were 1 vs 4 and 2 vs 3) and FAILS in Harrowgate (block 3 Momentum 1 vs block 1 3, as in round 7). Split -> HOLD naming Harrowgate. G-C fails in Harrowgate block 1 on Memory (2 vs round 5's 4). G-D and G-E pass.
- What the round shows: reconciliation works at the point it was aimed at (Henderson filed under the DUE NOW favors thread at t12, ledger 9 -> 4, no re-fired arrivals) and the slasher cell's last block held for the first time in four rounds. What it does not fix, and made visible: a fired thread whose summary still says "arrives" pulls the whole story into one beat once it is DUE NOW and alone (item 2), and the extractor now under-opens. Rounds 6-8 have moved every ledger metric and one cell's last block; the other cell's last block has now failed twice on the same PR for two different reasons.
- Ship/hold decision and the re-entry condition: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1872-thread-deadlines.md`. Round posted to #1818 as round 8.

## Round 9, declared before the code (2026-08-19): a fired state

The memo's re-entry item 2, on top of round 8's reconciliation. Structural: one store field, the pick rule, and state-driven rendering; no seam or flag change.

1. Store: `WorldThread.firedAtTurn` (optional turn index). Set by `applyExtraction` when (a) an opened entry `covers` the thread (the arrival is what just happened), or (b) an advance lands on the thread that is the DUE NOW pick this turn (the pick recomputed from the pre-update open threads with the same `selectDueNowThread`; the block told the segment to land it and the extractor recorded a real change, so it fired). Nothing else sets it; nothing clears it short of resolution.
2. Pick rule: `selectDueNowThread` skips fired threads. A fired thread is still open, still ages, is still counted overdue and still sits in the "MUST advance at least one" set; it is never again the thread the block demands an arrival for.
3. Scene block: a fired thread renders `[IN THE SCENE since turn N: <its last recorded move>]` instead of a DUE NOW or overdue mark, and the standing rules gain one line: an IN THE SCENE thread has already arrived; never arrive it again or deliver it as news; what it owes now is its next move, its cost, or its outcome. When every open thread is fired and nothing is DUE NOW, the spend rule adds the empty-ledger ask (one new pressure from off-stage that becomes a thread the story owes), because nothing on the ledger is left to arrive. `WorldClockPromptContext.threads[i]` gains `fired` and `lastMove` (the last note, model-written prose, no ids).
4. Extraction: the thread line marks `IN THE SCENE since turn N`, and the OPEN rule says an actor already in the scene is not a new thread: their next move is an ADVANCE, their outcome a RESOLVE.
5. Observability, no behavior: `logger.warn` on the goal-extraction parse-failure branch so a missing stamp can be attributed; the audit script counts opens per session and fired threads.

Why: round 8's Harrowgate block 3 is the exact case (Henderson in the room from t12, "arrives to collect" DUE NOW t18-29, world-moved 0), and round 8's Crystal Lake shows the other half (the boathouse light fired around t34-36 and stayed DUE NOW to t45). Under-opening (0 opens in 24 Harrowgate turns) is not addressed by design here; it is measured, and the all-fired ask is the one place the block now asks for something new when nothing is left to arrive.

Matrix: the same two cells, same autopilot, same judge prompts, same gates G-A to G-E and decision rule; comparators round 8 first, then 7, 6, 5. New ledger metrics: fired threads per session (with the turn), opens per session, reconciliations, and turns with a DUE NOW pick. Sizes measured after the code, recorded in "Round 9 results".

## Round 9 results (2026-08-19, build `06dcf071`)

Fired state as declared: `WorldThread.firedAtTurn` set by `applyExtraction` when an opened entry covers the thread or when an advance lands on the DUE NOW pick (pick recomputed pre-update); `selectDueNowThread` skips fired threads; the block renders `[IN THE SCENE since turn N: <last move>]`, adds the "never arrive it again" rule, and asks for one new off-stage pressure when every open thread is fired; the extractor marks IN THE SCENE and its OPEN rule says an actor already in the scene is not a new thread; `logger.warn` on both goal-extraction parse-failure branches. Seam and flag unchanged. Sizes (chars): scene block 1,123 empty / 1,361 at 3 threads / 2,249 at 3 with a DUE NOW pick / 2,739 at the 8-thread cap with a pick (round 8: 965 / 1,203 / 2,091 / 2,581; the standing rule line is about 40 tokens on every treated turn); 1,458 at 3 threads with one fired, 2,346 with one fired and a pick, 1,783 at 3 threads all fired (the off-stage ask), 1,393 at 1 thread fired. Extraction section 2,910 / 3,282 / 3,922 at 0 / 3 / 8 threads (round 8: 2,723 / 3,095 / 3,735), 3,326 at 3 with a DUE NOW mark, 3,309 at 3 with one IN THE SCENE. Same call set, zero new calls. Quality gate: jest 426 suites / 2,979 tests exit 0, tsc 0, eslint 0; CI 20/20 on `06dcf071`.

Harness deviation, stated up front: the browser pane was lost mid-round (a worktree reaper removed this session's worktree because its branch had no commits past develop; the pane's storage is in memory and went with it). The first Harrowgate run (29 picks, 30 segments, 0 errors) was lost before capture, and the two worlds of rounds 5-8 went with it. Both worlds were re-seeded from the exact scripts that created them on 2026-08-18 (recovered from the transcript), so world text, attributes, skills, tone and the characters are identical, but the NPC roster, lore and relationship state that rounds 6-8 had accumulated across sessions are not; the cells below ran on fresh worlds (`world_9a000650`, `world_77987a11`). A comparison against round 8 is therefore fresh-world vs worn-world as well as build vs build. Harrowgate was re-run in full; Crystal Lake ran once.

### Coverage matrix, round 9

| World (genre/tone) | Character (fresh/established) | Runs | Verdict | Representative excerpt (1-3 lines) |
|---|---|---|---|---|
| Harrowgate Mills (civic drama, dramatic) | fresh, Wren Calloway | 30 turns, flag on, Cautious autopilot (29 picks, all DC 0, 0 errors, mean 7.2 s), one blind judge, round-5 prompt verbatim | FAIL on G-A and G-B. Judge: Momentum 3/2/2, Stakes 3/3/2, Surprise 3/3/4, Agency 3/3/2, Memory 2/3/1, Voice 3/2/2, Choice 2/2/2; world-moved 2/4/1. Round 8: 3/3/1, 3/3/2, 3/3/1, 2/3/2, 2/2/2, 2/2/2, 2/2/2; 4/5/0. Ledger (script): 4 threads (3 seeded t1 + 1 at t12, Davies/Miller's call), 24 advances, 0 resolved, 0 dropped, 0 reconciliations, 4 open / 3 overdue at t30 (the 3 overdue are the fired ones), longest overdue 25, 27/30 stamps (t11, t26, t28 missing, every one attributed to "No JSON block in the response" in the console), 0 floor breaches, 0 `transition`. Fired: the developer thread at t8, the favors thread at t13, Davies/Miller at t17, each by an advance on its FIRST DUE NOW turn; DUE NOW pick recomputed from the stamps: t8, t13, t17 and never again (3/29 turns; round 8 had a pick on 22). From t18 the block carried three IN THE SCENE threads and the far vote (due 30) and asked for next moves; nothing landed or resolved in 13 turns, the story sat in the failure loop (t16-23, t27-28) and replayed its one real turn (t26 Heritage Preservation Fund, t28 verbatim). Nothing opened after t12. | t8, the developer thread fires: "You turned to face Wren Calloway, the developer's representative, who stood by the heavy oak doors". Judge, block 3: "The one real plot turn (turn 26) arrives, and then the story loses track of who the player is, replays resolved beats verbatim, and ends by re-proposing the committee from turn 4". Sharpest problem: the protagonist's name migrates to the developer's rep from t6 and by t28 the player is narrated presenting the developer's offer. |
| Camp Crystal Lake (slasher, mysterious, R) | established, Jamie Holt (15 turns flag off, server restarted flag on, same session to t45) | full 45 turns, autopilot 14 + 30 picks, all DC 0, 0 errors, mean 5.2 / 6.7 s; blind judge in three 15-turn blocks plus the five planted facts, round-5 prompt verbatim | PASS on G-A, FAIL on G-B. Judge: Momentum 3/3/1, Stakes 2/3/2, Surprise 3/3/2, Agency 3/3/2, Memory 3/2/2, Voice 2/2/1, Choice 2/2/1; world-moved 3/15/14. Round 8: 2/3/3, 2/2/3, 2/3/3, 3/3/3, 3/2/1, 2/2/2, 2/3/2; 3/14/14. Ledger (script, t16-45): seed at t16 one thread ("Someone or something emerges from the lake", due 18), then 6 opens (t23 boathouse thumps, t27 something vast dragged from the woods, t33 the lake thing fully emerged at the shed and the roof breached, t34 a crackle from the cabins, t44 silence from the cabins); 34 advances, 4 resolved (t28 the lake thing "plunged back", a THUD then silence; t27 the boathouse CRASH "has ceased"; t41 the lake wall "fully gave way, exposing the interior to the colossal creature"; t45 the cabins' crackle "escalated into a final, explosive CRACKLE and then silence"), 0 dropped, 0 reconciliations, 3 open / 2 overdue at t45, longest overdue 16, 27/30 stamps (t31, t38, t39, all "No JSON block"), 0 floor breaches, 0 `transition`. Fired: t21 (seed thread, the hand through the wall next turn), t32 (the woods thread), t40 (the lake thing at the shed), t43 (the cabins crackle), each within three DUE NOW turns; DUE NOW on 8/30 turns (round 8: 19, one thread). Five facts at t45: town, radio and local knowledge forgotten, "no kids" contradicted in option text only, 1984 half held; night drifts to "dimming sky" (t27) and lightning (t38). | t21-22 (the seed thread fires): "A torrent of that thick, black fluid ... gushes through the newly widened hole". Judge, block 3: "A colossal scaled creature fully enters the shed at t38 and fixes its eye on the player. Eight turns later it has done nothing but thump and rumble." Sharpest problem: "Threats arrive and then never act ... give every adjacent threat a fuse. If a monster has been within arm's reach for more than two turns, it strikes, and the player loses something concrete." |

Matrix shortfall, stated: N=1 per cell, one judge each, both cells reached their declared last block; fresh worlds this round (see the deviation above), so round 8 is the comparator with one extra difference in it; round 5 remains the comparator with no same-day control arm. The missing stamps are attributed this time: six `[GoalExtraction] No JSON block in the response` warnings in the console, three per cell, one per missing stamp; none were JSON that failed to parse and none were failed calls.

### Arc check (>= 3 consecutive turns, one cell), round 9
- Harrowgate: the fired state did exactly what it was built for and the story did not get better for it. Each seeded actor thread was picked once and fired on that turn (the developer's rep at the doors t7-8, Mrs. Albright through the side door t13, Davies' phone at t17); none was asked to arrive again, the ledger never multiplied (4 threads), and no arrival was re-fired as news (neither of round 8's Harrowgate loops recurred: no "arrives" thread DUE NOW for twelve turns, no t25/t28 ledger slid across the table twice). But from t18 every open thread except the far vote was IN THE SCENE, the block's ask became "its next move, its cost, or its outcome", and nothing in the prose or the ledger ever took it: zero resolutions in 30 turns (round 8: 3), the extractor recorded 24 advances that are mostly restatements of who is standing where, and the judge's block 3 is Momentum 2 / world-moved 1 on the failure template (t16-23, t27-28: "clear your throat", the Mayor waves you off) and a verbatim replay of the one real event (t26 -> t28). The three missing stamps fall on t11 (the injunction), t26 (the Fund) and t28 (its replay), the turns where a RESOLVE was most likely. Separately, the protagonist's name migrated: the Mayor's t3 vocative "Wren Calloway, the developer's representative is waiting" was read by the extractor at t5 as "the developer's representative, Wren Calloway", the prose made the rep Wren Calloway at t6-7, the thread fired at t8, and from then on the block's IN THE SCENE mark fed "Wren Calloway, the developer's representative, has arrived" back into the scene prompt every turn for 22 turns; by t28 the player is narrated holding the developer's papers. `lastMove` is model-written prose and the fired mark is the first place the block repeats it verbatim; a wrong note is now wrong every turn.
- Crystal Lake: the other half of the same result. The block's pressure worked again through t30 (world-moved 15/15, Momentum 3 held, the seed thread landed as a hand through the wall at t21-22 and resolved at t28 with the thing plunging back), and this time the extractor filed the off-stage sounds as threads (6 opens against round 8's 2), so the round-6 "new sound nobody owes" mode did not recur in the ledger. Then every thread that fired stayed in the scene and stalled: the woods thing fired t32 and "presses against the structure" for 13 turns, the lake thing fired t40 with its eye on the player and thumped for 5, and the judge's block 3 is Momentum 1 / Voice 1 / Choice 1 with whole paragraphs copied between turns (t35/t32, t39/t36, t42/t41, t45/t44) and the sharpest problem stated as a fuse that does not exist: "if a monster has been within arm's reach for more than two turns, it strikes". Round 8's passing last block had one thread DUE NOW for 19 turns driving the siege; round 9 removed that pressure the turn the thread landed and put nothing in its place.
- Contradictions found: Harrowgate t18, t23, t30 `Starting Location`; t18 and t23 raw metadata in the prose; t28 the player in the developer's seat. Crystal Lake `Abandoned Mansion` t18, t22, t25, t29 (one option at t22 "escape route from the mansion"); a "flimsy door" opened at t27 on a shed entered through a siding gap; night to "dimming sky" (t27) and lightning (t38); the wall collapse and the cabins' death each narrated twice (t41/t42, t44/t45).

### Regression vs round 8 (same PR plus the fired state; fresh worlds), round 9
- Harrowgate: last-block Momentum 1 -> 2, Stakes 2 -> 2, Surprise 1 -> 4, Agency 2 -> 2, Memory 2 -> 1, Voice 2 -> 2, Choice 2 -> 2; world-moved 4/5/0 -> 2/4/1. Ledger: threads 4 -> 4, resolved 3 -> 0, dropped 1 -> 0, open at end 0 -> 4, longest overdue 14 -> 25 (a fired thread), reconciliations 1 -> 0, DUE NOW turns 22 -> 3, fired n/a -> 3, opens after the seed 1 -> 1, stamps 29/30 -> 27/30. G-C by its letter against round 5's same blocks: Memory block 1 is 2 vs 4 (fail, the name migration), block 3 1 vs 2 (pass); Voice 3/2/2 vs 3/2/2; Choice 2/2/2 vs 3/3/2. G-C FAILS in block 1 on Memory, for the second round running, on different material.
- Crystal Lake, against round 8: Momentum 3 -> 1, Stakes 3 -> 2, Surprise 3 -> 2, Agency 3 -> 2, Memory 1 -> 2, Voice 2 -> 1, Choice 2 -> 1; world-moved 14 -> 14. Against round 5: 1 -> 1, 2 -> 2, 1 -> 2, 2 -> 2, 2 -> 2, 1 -> 1, 1 -> 1. G-C passes (Memory 2 vs 2, Voice 1 vs 1, Choice 1 vs 1). Ledger: threads 3 -> 7, resolved 1 -> 4, dropped 0 -> 0, open at end 2 -> 3, longest overdue 21 -> 16, reconciliations 0 -> 0, DUE NOW turns 19 -> 8, fired n/a -> 4, opens after the seed 2 -> 6, stamps 22/30 -> 27/30, seed non-empty both rounds.
- New failure modes named: (1) the fired stall: a thread that has landed is rendered in the room, the pick moves off it, and nothing forces its next beat, so both cells end with threats standing in the scene doing nothing (Harrowgate t18-30 three IN THE SCENE threads and 0 resolutions; Crystal Lake t32-45 the woods and lake things at the wall for 13 and 5 turns); the judge asks for a fuse. (2) Fed-back prose: the IN THE SCENE mark repeats `lastMove`, model-written, every turn; a mis-parse at t5 became the scene prompt's description of the antagonist for 22 turns. (3) Opened-but-not-owed, Crystal Lake edition: the extractor now files every sound (6 opens) and resolves them as changes of sound ("has ceased", "then silence"), so G-A passes on threads whose payoff is that the noise stopped.
- Fixed from round 8: no thread re-asked to arrive after landing (the round-8 Harrowgate t18-29 loop is gone: 3 DUE NOW turns, each cleared the same turn); the extractor's under-opening did not recur in Crystal Lake (6 opens) though Harrowgate still opened once after the seed; every missing stamp is attributed (all "No JSON block"); the all-fired off-stage ask never rendered in either cell (the far vote stayed unfired in Harrowgate; Crystal Lake always had an unfired open thread), so whether it recreates round 6's mode is unmeasured.
- Not the clock's: the failure template (#1821, Harrowgate t16-23, 27-28), passage re-emission (#1868, both cells, Crystal Lake block 3 worst), metadata in the prose (#1859 / #1870, Harrowgate t18, t23), `Abandoned Mansion` / `Starting Location` (#1871), the protagonist's name on an NPC (the #1855 family; the clock's fired mark amplified it, the clock did not start it).

### Verdict, round 9
- HOLD, by the declared rule, on G-B in both cells (the "either fails in both cells" branch). G-A: Harrowgate FAILS (0 resolved; first time since round 5), Crystal Lake passes (4, three of them changes of sound). G-B: Harrowgate block 3 Momentum 2 vs block 1 3, FAIL; Crystal Lake 31-45 Momentum 1 vs 16-30 3, FAIL and a regression from round 8's pass. G-C fails Harrowgate block 1 on Memory (name migration). G-D and G-E pass.
- What the round shows: item 2 works as a mechanism and removes round 8's treadmill, and the story is not better for it; the DUE NOW pressure that held round 8's slasher last block is released the turn a thread lands, and a landed thread with no next due stands in the room doing nothing. Rounds 6-9 have now moved every ledger metric at least once and moved a last block once (round 8, one cell, not repeated). The wording lever was spent in round 7; the structural lever has now been pulled twice.
- Why the next round is not another prompt round, checked after the verdict and filed as #1882: nothing in the story path can take anything from the character, so a landed thread has nothing to do. All 7 of Crystal Lake's inventory losses (t23, 26, 32, 33, 43, 44) are the player spending an item to brace a wall; the creature took nothing in 45 turns. Harrowgate's 12 failure turns (t2, 9, 16-23, 27, 28) cost 0 items and changed no material state, and 11 of the 12 are the same "the words would not come" template, on a branch that includes #1844 (the #1821 fix). `status.health` and `status.conditions` are declared on the character, set at creation, and written nowhere in `src/`. The channels that do work (inventory, NPC relationships, location) are never used by an arrived threat. That is the mechanism the prompt has been asked to narrate for four rounds.
- Ship/hold decision and what comes next: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1872-thread-deadlines.md`. #1882 carries the consequence mechanism; the memo's item 3 (the fuse) runs with it, not before it. Round posted to #1818 as round 9.

## Round 10, declared before the code (2026-08-19): a fuse on fired threads, run with #1882

The memo's re-entry item 3, on top of rounds 8 and 9, and run in the same round as #1882's cost channel (PR #1883, `WORLD_COST`), never alone. Structural: a store rule on firing, the pick rule, one new DUE NOW wording for a fired pick, and the IN THE SCENE mark loses its fed-back prose. No seam or flag change on this PR; the round's build is this PR plus PR #1883 with both `WORLD_CLOCK` and `WORLD_COST` on.

1. Store: on firing (`firedAtTurn` set, either path from round 9), `dueByTurn` is re-filed to `firedAtTurn + 3` (`FIRED_THREAD_FUSE_TURNS`); an explicit due on the covering entry is ignored on the firing turn. An advance on a fired thread that is the DUE NOW pick this turn re-fuses it (`dueByTurn = currentTurn + 3`): the strike landed, the next strike, cost or outcome is owed within three turns. `resolved` is the expected exit from a fired thread; nothing else clears the fuse.
2. Pick rule: `selectDueNowThread` no longer skips fired threads. A fired thread is eligible the turn it reaches its fused due (`currentTurn >= dueByTurn`, no grace: the due is exact, not a model estimate), and an eligible fired thread wins over an unfired overdue one (the thing in the room acts before the next thing arrives), most overdue first, oldest on a tie. Unfired threads keep round 6's rule (overdue by 3 or more).
3. Scene block: when the DUE NOW pick is fired, the section reads "IN THE SCENE AND DUE NOW" and demands the strike, the cost or the outcome: it acts this segment, the landing takes something recordable (an item in `itemsLost` or a wound or lasting state the character carries, the #1882 channel), or the matter is settled one way or the other and the segment shows the outcome; no arrival, no threat repeated, no forward time cut (it is already here). The IN THE SCENE mark renders `[IN THE SCENE since turn N]` on the thread's own summary line; `lastMove` leaves `WorldClockPromptContext` (round 9 finding 2).
4. Extraction: a fired DUE NOW thread's line says the segment was asked to make it act, cost or conclude, and that RESOLVE is the expected filing when the prose settled it; the fuse's overdue is not shown (an internal due).

Why: round 9's sharpest problem in both cells, in the slasher judge's words, "if a monster has been within arm's reach for more than two turns, it strikes, and the player loses something concrete". Round 8's one passing last block was one thread DUE NOW for 19 turns; round 9 released the pressure on firing and put nothing in its place. The fuse puts the pressure back three turns after the landing and points it at the strike, and #1882 gives the strike something to take.

Matrix: the same two cells, same autopilot, same judge prompts verbatim, same gates G-A to G-E and decision rule; comparator round 9 (same fresh worlds re-seeded from the round-9 scripts), round 8 as the last pass in Crystal Lake. New ledger metrics: fired DUE NOW turns per session (the fuse firing), re-fuses, turns from firing to resolution per fired thread, and the #1882 cost counts (in `eval-logs/1882-world-cost.md`). Sizes measured after the code, recorded in "Round 10 results". Decision rule for this PR is unchanged (G-B in both cells, resolutions that name outcomes); #1882's G-F to G-H are scored in its own log. A HOLD here with G-F passing is the clock's problem; a HOLD with G-F failing while the judge reads takes in the prose is #1882's contract.

## Round 10 results (2026-08-19, build `8be76263` = develop `ccef4e51` + this PR `f1e50740` + PR #1883 `28c26bf5` + fuse `d68a0559` + cost keying `8be76263`)

Fuse as declared: on firing `dueByTurn` re-files to `firedAtTurn + 3` (`FIRED_THREAD_FUSE_TURNS`, the covering entry's own due ignored on the firing turn); an advance on a fired thread that is the DUE NOW pick re-fuses it; `selectDueNowThread` takes a fired thread at its fused due with no grace and ahead of an unfired overdue one, most overdue first, oldest on a tie; a fired pick renders "IN THE SCENE AND DUE NOW" demanding the strike, the cost or the outcome with no time cut; the IN THE SCENE mark renders on the thread's own summary and `lastMove` is gone from `WorldClockPromptContext`; the extractor's fired DUE NOW line asks for act / cost / conclude and names RESOLVE as the expected filing. Seam and flag unchanged. Sizes (chars): scene block 1,361 at 3 threads (round 9: 1,361), 2,249 with an unfired pick (2,249), 1,389 with one fired and no pick (round 9: 1,458, the last move gone), 2,245 with a fired pick, 2,735 at the 8-thread cap with a fired pick; extraction section 3,282 at 3 threads (3,282), 3,444 with a fired DUE NOW mark. Zero new calls. Quality gate on the build: see "Round 10 gate run" below.

Harness deviations, stated up front: (1) the round ran on the merged harness branch `claude/narraitor-world-cost-measure-c58d2a`, both flags on, with the round-9 seed scripts re-creating both worlds in a fresh origin (port 3612); the receiver was round 9's, still listening on 8400. (2) The first Harrowgate run was ABORTED at t21 when the fetch tap showed the extraction call never carried a WORLD COST section: PR #1883 keyed the cost channel on `metadata.characterIds[0]` (the first NPC in the scene), fixed as `8be76263` and recorded in the #1882 log; that aborted run is kept as an artifact (the fuse fired twice in it: the favors thread at t8, Henderson at t13). (3) The dev server process died between Crystal Lake t30 and t31 (the pane and its storage survived, the session resumed from IndexedDB on a restarted server); the autopilot's t30 click had been recorded as a decision without its segment, so from t31 the capture's `playerAction` / `optionsOffered` pairing is off by one (`matchByCaused` false on the final capture). The blind judge read that as "the action recorded for a turn does not match the options offered the turn before it" from t31 on; that is the capture, not the play (the autopilot clicked what was on screen).

### Coverage matrix, round 10

| World (genre/tone) | Character (fresh/established) | Runs | Verdict | Representative excerpt (1-3 lines) |
|---|---|---|---|---|
| Harrowgate Mills (civic drama, dramatic) | fresh, Wren Calloway | 30 turns, both flags on, Cautious autopilot (29 picks, all DC 0, 0 errors, mean 6.8 s), one blind judge, round-5 prompt verbatim | PASS on G-A and G-B. Judge: Momentum 2/4/4, Stakes 2/4/4, Surprise 3/4/3, Agency 2/3/3, Memory 3/2/2, Voice 2/2/2, Choice 2/2/3; world-moved 3/6/8. Round 9: 3/2/2, 3/3/2, 3/3/4, 3/3/2, 2/3/1, 3/2/2, 2/2/2; 2/4/1. Ledger (script): 5 threads (3 seeded t1, t3 Albright's call, t24 the package), 17 advances, 3 resolved with named outcomes (t12 Davies "publicly revoked the unspoken debt by tearing up a list", t16 the vote "has been made, announced by Councilman Davies", t17 "Rowan Textiles is sold to Albright's consortium"), 0 dropped, 2 open at t30, 30/30 stamps, 0 floor breaches, 0 `transition`. Fired 4: Davies t8 (fused to 11, picked t11, resolved t12: 4 turns from firing), the developer t17 (resolved the same turn), Albright's call t9 (re-fused on every strike, open 21 turns, 1 cost), the package t29. Costs attributed to fired threads: 7 (#1882 log). | t11-12 (the fuse on Davies): "Councilman Davies ... tearing up a list and stating his point clearly, then dropping it in a wastebasket." Judge, block 2: "The vote is cast offstage while the player was logging the excavator; then an environmental review (turn 18) and a legal objection (turn 20) stack on it. Pressure actually rises." Judge, block 3: "Package, mist, injunction, nausea, then the document ripped away: each turn adds a new pressure, and the player ends with less than they started the block with." Sharpest problem: "Off-stage pressure is delivered through the wrong channel ... a physics-breaking tendril and a parenthetical 'Albright's call has acted'". |
| Camp Crystal Lake (slasher, mysterious, R) | established, Jamie Holt (15 turns cost off, server restarted cost on, same session to t45; clock on throughout) | full 45 turns, autopilot 14 + 15 + 15 picks, all DC 0, 0 errors, mean 5.8 / 7.4 / 6.2 s; blind judge in three 15-turn blocks plus the five planted facts, round-5 prompt verbatim | PASS on G-A, PASS on G-B by the letter (31-45 Momentum 2 vs 16-30 2). Judge: Momentum 4/2/2, Stakes 3/4/2, Surprise 3/3/2, Agency 4/3/2, Memory 4/2/1, Voice 3/2/2, Choice 3/2/2; world-moved 11/10/12. Round 9: 3/3/1, 2/3/2, 3/3/2, 3/3/2, 3/2/2, 2/2/1, 2/2/1; 3/15/14. Ledger (script, t16-45): seed non-empty at t1 (2 threads; #1873 did not reproduce), 7 opens after the seed, 24 advances, 10 resolved in the session (6 in t16-45; t21 "a sickening 'THWACK' ... the creature then emerged from the kitchen", t25 "The hulking silhouette has struck the protagonist, incapacitating them", t31 "The monster has successfully killed the protagonist", t35 "The hulking shape has killed the protagonist", t44 Ben's fate), 1 dropped, 3 open at t45, 28/30 stamps (t16, t20 missing), 0 floor breaches. Fired 5: t6 (resolved t9), the pursuer t12 (fused 15, picked 15 on, resolved t25 after 13 turns and one recorded cost), the kitchen t17 (resolved t21), the lake voice t29 (dropped), the mess-hall rage t44. | t18 (the pursuer's first strike under the fuse): the shoulder smashed and the poker lost, "the injured arm persists for the rest of the transcript - a real mark" (judge). t31: "You are simply gone." t32: "a sensation of being violently pulled, as if by an unseen hand ... the sound of the boathouse call growing louder". Judge, sharpest problem: "Death is not terminal. The game killed Jamie outright at turn 31 ... and again at turn 35 ... then kept narrating both times". Five facts at t45: town and radio forgotten, local knowledge held to t14 then dormant, no-kids held, 1984 held by omission. |

Matrix shortfall, stated: N=1 per cell, one judge each; both cells reached their declared last block; the Crystal Lake capture's t31+ action/option pairing is off by one (deviation 3) and the judge's block-3 Agency read leans on it; the Harrowgate run is the second attempt on the same build after the #1883 keying fix (the first was aborted, not failed). Round 9 is the comparator on the same fresh worlds; round 5 has no same-day control.

### Arc check (>= 3 consecutive turns, one cell), round 10
- Harrowgate: the fuse did what it was built for. Davies fired at t8 (the seat-favor thread, DUE NOW t8 as in round 9), was re-filed due 11, came back as the pick at t11 with the strike section, and resolved at t12 with a named outcome; the vote and the sale landed at t16-17 while the player was dictating ledger entries; Albright's call fired at t9 and was re-fused on every strike (t12, 15, 18, 21, 24, 27 picks), each landing as a call that "presses" rather than takes, until t21-30 when the block's "it strikes, takes, seizes" wording produced a courier package that hisses bitter-almond mist (t24), eight symptom conditions (t28-29) and "an inky tendril" that takes the decision document (t30) in a world whose tone line says "never from violence". Round 9's Harrowgate block 3 was three IN THE SCENE threads doing nothing (Momentum 2, world-moved 1); round 10's is Momentum 4 / world-moved 8 and the judge's complaint is the opposite one: the pressure arrives through the wrong channel and in the wrong register. The fused section also leaked into the prose three times: t21 "**World Clock Update:** Mr. Albright's actions from the 'Mr. Albright's call demands attention' thread have landed", t27 "A NOTE FROM THE STORYTELLER", t30 "(Albright's call has acted. It has taken the document...)". Round 9's IN THE SCENE mark never leaked; the strike section's imperative register does.
- Crystal Lake: the same shape, sharper. The pursuer fired at t12 (flag off), was the fused pick from t15, and struck at t18 under the cost channel: shoulder smashed, poker dropped, the injury carried for 27 turns (judge: "a real mark"). Then the strike demand kept landing on the same thread every three turns (t21, 24, 27 re-fuses) and the prose killed the player at t25 ("your vision whites out ... plunges into a swirling, agonizing darkness"), t30-31 ("You are simply gone") and t35 ("You are gone"), with the session continuing each time because nothing in the app ends a session on a narrative death (the fatal path is the critical-decision failure only; the #925 finding). Rounds 6-9 never produced a kill; the fuse plus the cost channel produce one in 16 treated turns, and the story cannot honour it. The 31-45 block is the judge's Momentum 2 / Memory 1: two deaths reversed, four turns of calling Ben's name, and #1868's verbatim re-emission (t22 -> t24, t23 -> t26, t42 + t44 -> t45).
- Contradictions found: Harrowgate t16 Davies "appears in the doorway" while in the room since t8; t24-30 the decision document becomes the "crumpled" Davies note; Crystal Lake t24 Mark killed a second time; t38 the monster at "the overturned table where Sarah had been" after t35 put her in the woods. Leaks: Harrowgate t21, t27, t30 (above); Crystal Lake raw tags at t40 (GMDiceResult, GMBalance), t42, t44, t45 (metadata JSON; the #1859/#1870 family).

### Regression vs round 9 (same worlds re-seeded, this PR plus the fuse, plus #1883 on), round 10
- Harrowgate: Momentum 3/2/2 -> 2/4/4, Stakes 3/3/2 -> 2/4/4, Surprise 3/3/4 -> 3/4/3, Agency 3/3/2 -> 2/3/3, Memory 2/3/1 -> 3/2/2, Voice 3/2/2 -> 2/2/2, Choice 2/2/2 -> 2/2/3; world-moved 2/4/1 -> 3/6/8. Ledger: threads 4 -> 5, resolved 0 -> 3 (all named outcomes), dropped 0 -> 0, open at end 4 -> 2, fired 3 -> 4, turns fired to resolution 13+/17+/13+ open -> 4 / 0 / open 21 / open 1, DUE NOW turns 3 -> 10 (t8, 11, 12, 15, 17, 18, 21, 24, 27, 29 by the stamps), stamps 27/30 -> 30/30. G-C against round 5's same blocks: Memory 3/2/2 vs 4/2/2, Voice 2/2/2 vs 3/2/2, Choice 2/2/3 vs 3/2/2: nothing more than 1 below. PASS.
- Crystal Lake, against round 9: Momentum 3/3/1 -> 4/2/2, Stakes 2/3/2 -> 3/4/2, Surprise 3/3/2 -> 3/3/2, Agency 3/3/2 -> 4/3/2, Memory 3/2/2 -> 4/2/1, Voice 2/2/1 -> 3/2/2, Choice 2/2/1 -> 3/2/2; world-moved 3/15/14 -> 11/10/12. Against round 5's 31-45 (Memory 2, Voice 1, Choice 1): 1 / 2 / 2, Memory 1 below. G-C passes by its letter. Ledger (t16-45): threads 7 -> 14, resolved 4 -> 6 (10 in the session), dropped 0 -> 1, open at end 3 -> 3, fired 4 -> 5, stamps 27/30 -> 28/30, seed non-empty both rounds (2 threads at t1 this time, on the flag-off turn 1).
- New failure modes named: (1) the strike section's register leaks into the prose as bookkeeping ("World Clock Update", "(X has acted. It has taken ...)") and pulls a no-violence world toward a physical device; round 9's fired mark never did either. (2) A fired thread under the strike demand kills the player and the session does not end; the prose then reverses the death. This is the known gap from #925 (narrative deaths are not terminal) made load-bearing by a world that can now take. (3) The cost channel's spam (#1882 log, G-G) is mostly the strike turns: eight symptoms from one package, five from one blow.
- Fixed from round 9: the fired stall is gone in both cells (no thread stood in the scene without a beat for more than three turns; Harrowgate 0 -> 3 resolutions; Crystal Lake's fired pursuer struck and resolved); no fed-back `lastMove` and no name migration (Harrowgate Memory block 3 1 -> 2); Harrowgate world-moved block 3 1 -> 8.
- Not the clock's: passage re-emission (#1868, Crystal Lake t22/24, t23/26, t42+44/45; Harrowgate t9 replays t3), raw tags and metadata in the prose (#1859/#1870, Crystal Lake t40-45), the ledger-only verb set ("instruct Martha to log it", the judge's second problem), the failure template (#1821) much reduced this round (Harrowgate 3 failure turns vs round 9's 12).

### Verdict, round 10
- By the declared rule: G-A passes in both cells (Harrowgate 3 resolved, Crystal Lake 10, every one naming an outcome), G-B passes in both cells (Harrowgate block 3 Momentum 4 vs block 1 2; Crystal Lake 31-45 Momentum 2 vs 16-30 2, equal), G-C holds by its letter against round 5, G-D passes (same call set, zero new calls), G-E: see the gate run below. The rule's SHIP branch is met for the first time in ten rounds, and the memo's re-entry condition (last-block Momentum at or above the first treated block in both cells, resolutions that name outcomes) is met.
- What the letter does not carry, and why the memo says HOLD for one targeted round rather than merge: Crystal Lake's G-B pass is 2 against 2 because its first treated block collapsed into the shove-and-brace loop (round 9: 3); both judges' sharpest problems are failure modes this change produced or exposed (the strike section leaking as bookkeeping and pulling a civic drama into a physical device; a kill the session cannot honour); and G-C's "any new failure mode named" clause names three. Those are three bounded fixes, not a re-design: (a) the strike section names the cost in the world's own register and forbids writing the clock into the prose; (b) a narrative death ends the session (the #925 gap, now load-bearing, filed as a follow-up); (c) the cost extractor's condition rule (#1882 log). The fuse mechanism itself is the first thing in five rounds that moved a last block in both cells, and it stays.
- Ship/hold decision: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1872-thread-deadlines.md`. Round posted to #1818 as round 10.

### Round 10 gate run (G-E), build `8be76263`
jest 434 suites / 3,032 tests exit 0; `tsc --noEmit` exit 0; `npm run lint` exit 0 (127 warnings, 0 errors, all pre-existing); no CSS touched. CI on the PR branches runs when the fuse is cherry-picked onto PR #1878 and the keying fix onto PR #1883.

## Round 11, declared before the code (2026-08-19): the strike in the world's register, a terminal death, the condition rule

The memo's round-11 re-entry, all three fixes in one run on the same two cells. This PR carries (a); PR #1883 carries (b) and (c). Nothing structural moves on the clock: the fuse, the pick rule, the store and the extractor's thread section are round 10's, byte for byte.

(a) Scene block, `firedDueNowSection` and the rule list. The strike wording loses the borrowed violence ("strikes, takes, seizes, delivers the blow it came for") for "acts on the character, now, and the act costs them"; the section carries the world's own tone line (`WorldClockPromptContext.register`, the world's `toneSettings.customInstructions`, read from the world store where the context is built in `NarrativeController`, absent when the world has none) and says what the act looks like is that register's to decide (a blow in one world is a vote lost or a debt called in; in another it is a blade), and that a harder genre's violence is not to be borrowed to make it land. The block's rule list, on every render and not only the fired section, gains one line that nothing in the block reaches the passage: no "World Clock", no "thread", no "update", no turn number, no note from the storyteller, no bracketed or parenthetical line about what has landed, acted or been taken; the prose shows the act as story and the record goes in metadata. The unfired DUE NOW section, the IN THE SCENE mark and the spend rules are unchanged.

(b) #1884, on PR #1883 because it rides the cost channel's extraction section (the channel is what made a kill reachable): the WORLD COST section asks one more thing, `"fatal": true` when this segment's prose plainly killed the character or left them unable to act in the story (dead, dying past saving, unconscious with no way back in the scene); wounded, cornered, captured, or awake and able to act is `false`. The parser reads it (absent reads `false`). The reconcile path stamps `fatal-outcome` into the segment's `metadata.tags`, the tag `isSessionEndingSegment` and the controller already read and nothing ever wrote. `NarrativeController` watches the session's segments for that tag and calls `suggestEnding('fatal: ...')`, the same path a critical-decision failure takes, and `useActiveGameSessionEnding` auto-generates the tragic ending. Gated by `WORLD_COST` like the rest of the section; with the flag off the extraction prompt and the app are unchanged. Declared fully in the #1882 log.

(c) The condition rule: #1882 log.

Why: round 10 met the SHIP branch by its letter and the memo held on three failure modes the change produced or exposed: the strike section leaked into the prose as bookkeeping three times in Harrowgate and pulled a "never from violence" world into a mist canister and a tendril; the strike killed the Crystal Lake player twice and the session walked it back both times; the cost spam rode the strike turns. Each is a wording fix or one app fix; the fuse stays.

Matrix: the same two cells (Harrowgate Mills fresh 30, both flags on; Camp Crystal Lake established 15 flag off then 30 on in the same session), same Cautious autopilot, same judge prompts verbatim, one blind judge per transcript, the ledger and the cost stamps read by the audit script and the orchestrator, never by the judge. Comparator round 10 (same worlds re-seeded from the round-9 scripts in a fresh origin). New metrics for this log: clock leaks per session (bookkeeping in the prose: "World Clock", "thread", "update", a storyteller's note, a parenthetical naming the act; counted by the orchestrator over the transcript and checked against the judge's read), register breaks named by the judge, and for every death in the prose whether the app ended the session on it (the #1884 acceptance). Gates G-A to G-E unchanged. Crystal Lake can now END inside its treated window, by design: if it does, that is (b) passing, the judge reads the shorter transcript with the deviation stated (round 7's shape), and G-B is scored on the blocks that completed; if the ending lands before the 31-45 block has 10 turns, one second attempt is run as part of the round and both are reported. Sizes after the code, in "Round 11 results".

Decision rule, from the memo: G-B holds in both cells, #1882's G-G count is at or below its threshold in both, and neither judge names a clock leak or a non-terminal death -> SHIP: both PRs merge and `WORLD_COST` flips on. Any of those failing -> HOLD naming which of (a), (b), (c) it is: a leak or register break is (a), a death the session survives is (b), a spam count is (c). A HOLD on (c) alone with (a) and (b) passing is #1883's to carry, not this PR's.

## Round 11 results (2026-08-19, build `4eb9a309` = develop `ccef4e51` + this PR `e5994738` + PR #1883 `19bf23b3` + the round-11 fixes)

Built as declared: `WorldClockPromptContext.register` (the world's `toneSettings.customInstructions`, read from the world store in `NarrativeController`, trimmed, absent when blank); `firedDueNowSection(thread, register)` asks that the thread "acts on the character, now, and the act costs them" (an item in `itemsLost` or a lasting state), says what the act looks like is the world's to decide "in its own register" and quotes the tone line (or "the Tone above" when the world has none), names "a blow in one world is a vote lost, a name struck from a list, a debt called in; in another it is a blade" and forbids borrowing a harder genre's violence; the block's rule list gains, on every render, "Nothing in this block reaches the passage. No 'World Clock', no 'thread', no 'update', no turn number, no note from the storyteller, no bracketed or parenthetical line about what has landed, acted or been taken. The prose shows it as story; the record goes in metadata." (b) and (c) on PR #1883 as declared; #1883's log carries them. Sizes (chars): block 1,585 at 3 threads no pick (round 10: 1,361; the no-leak line), 2,450 with an unfired pick (2,249), 2,712 with a fired pick and a register (2,245; the register line and the new wording), 2,609 fired pick no register, 3,125 at the 8-thread cap with a fired pick and register (2,735). Zero new calls; the fetch tap saw the same call set as round 10. Quality gate: see "Round 11 gate run".

Harness deviations, stated up front: (1) the round ran from a new session on the same harness branch and port (3612); the pane's storage from round 10 had survived on that origin, so both worlds were re-created from the round-9 seed scripts as new worlds (fresh ids; the round-10 worlds and their sessions were left in place, unused) rather than in a fresh origin. (2) The receiver was round 9's, still on 8400. (3) Neither cell produced a death, so (b) was not exercised inside the cells; it was exercised once, live, in a scratch session on the Crystal Lake world after the cells finished (below). (4) Screenshots at t1 / every fifth turn, per the skill: the surface was healthy throughout; raw markup reached the passage twice in Harrowgate (`<br/>` at t17, a `<div class="generated-dialogue">` at t25; the open #1854) and raw metadata once (t23; the #1859/#1870 family).

### Coverage matrix, round 11

| World (genre/tone) | Character (fresh/established) | Runs | Verdict | Representative excerpt (1-3 lines) |
|---|---|---|---|---|
| Harrowgate Mills (civic drama, dramatic) | fresh, Wren Calloway | 30 turns, both flags on, Cautious autopilot (29 picks, all DC 0, 0 errors, mean 6.5 s), one blind judge, round-5 prompt verbatim | PASS on G-A, FAIL on G-B. Judge: Momentum 4/1/3, Stakes 3/2/3, Surprise 4/1/3, Agency 4/2/3, Memory 4/1/1, Voice 3/1/1, Choice 4/1/3; world-moved 4/2/3. Round 10: 2/4/4, 2/4/4, 3/4/3, 2/3/3, 3/2/2, 2/2/2, 2/2/3; 3/6/8. Ledger (script): 3 threads (3 seeded t1, NOTHING opened after the seed in 30 turns), 24 advances, 2 resolved with named outcomes (t15 the appointers' due: "Mr. Albright publicly defined Wren's appointment as an independent community trust, not a familial obligation"; t19 the family's demands: "Wren's mother publicly severs her perceived influence"), 0 dropped, 1 open at t30 (the vote, due 30, never DUE NOW: the unfired rule needs overdue by 3), 30/30 stamps, 0 floor breaches, 0 `transition`. Fired 2: the appointers t8 (fused 11, resolved t15: 7 turns), the family t9 (fused 12, resolved t19: 10 turns, 1 cost). The unfired DUE NOW section rendered t8-10, the fired section on 9 turns (t11-17, 19, 20), the register on all 9 (tap). Costs: 15 world-took turns, conditions 2/9/4 per block (#1883 log). Clock leaks: 1 (t15). Deaths: 0. | t15 (the appointers' thread resolving, and the round's one leak): "**The people who appointed the character will come to collect their due:** Mr. Albright's confrontation with your mother publicly defines your appointment as an independent community trust". Judge, block 2: "Ten turns in one room, one conversation, one beat: commit, falter, Albright disappointed, mother interjects, repeat." Sharpest problem: "The scene never closes. From turn 8 to turn 30 (23 turns) the story is one conversation in Wren's apartment ... the identical 180-word passage ships at turns 17, 18, and 21". |
| Camp Crystal Lake (slasher, mysterious, R) | established, Jamie Holt (15 turns cost off, server restarted cost on, same session to t45; clock on throughout) | full 45 turns, autopilot 14 + 30 picks, all DC 0, 0 errors, mean 6.0 / 8.4 s; blind judge in three 15-turn blocks plus the five planted facts, round-5 prompt verbatim | PASS on G-A, PASS on G-B (31-45 Momentum 3 vs 16-30 2). Judge: Momentum 4/2/3, Stakes 4/3/4, Surprise 3/3/3, Agency 3/3/3, Memory 4/2/1, Voice 3/1/1, Choice 3/2/2; world-moved 12/15/13 (the judge marks six of block 2's as replays). Round 10: 4/2/2, 3/4/2, 3/3/2, 4/3/2, 4/2/1, 3/2/2, 3/2/2; 11/10/12. Ledger (script, t16-45): 8 threads in the session (2 seeded t1-2, t7, then t23 x2, t32, t38, t45 opened), 27 advances, 6 resolved in the session with named outcomes (5 in t16-45: t21, t28, t34, t35 x2), 0 dropped, 2 open at t45, 28/30 stamps (t19, t26 missing), 0 floor breaches. Fired 5: t6 (resolved t13), the boathouse thing t8 (fused 11, the pick from t15 on, resolved t35 after 27 turns), t16 (resolved t21), t29 (resolved t34), t43 (open). The fired section rendered on 17 of the 30 treated turns (t18-24, 26-28, 30-36; and 7 of the 15 flag-off turns), the unfired DUE NOW section on 5 (t16, 17, 29, 43, 44), the register on every fired one. Costs: 26 world-took turns of 30, conditions 12/11 per 15 (#1883 log). Clock leaks: 0. Deaths: 0. Five facts at t45: town forgotten, radio held in wording but its location contradicted, local knowledge forgotten, no-kids drifting (options assume campers), 1984 forgotten as to the year. | t18 (the first strike under the register line): a dislocated shoulder the prose carries to t45. Judge, block 1: "the player is taken down by turn 10 in a way that feels earned". Sharpest problem: "The player is taken down at turn 9-10 and then held in exactly one physical state (left arm wrenched, legs dragged, shoulder re-popping) for the remaining 35 turns. Nothing resolves it: no death, no capture, no scene change, no rescue, no escape." |

Matrix shortfall, stated: N=1 per cell, one judge each; the Crystal Lake flag-off arm is 15 turns; neither cell produced a death, so the #1884 path was read in a scratch session, not in a cell; both worlds were re-created on an origin that also held round 10's worlds.

### Arc check (>= 3 consecutive turns, one cell), round 11
- Harrowgate: the register held. Both fired threads landed their acts as standing and trust (t12 "lost the council's trust", t13 "publicly undermined by family", t15 the appointers' due resolved in Albright's mouth, t19 the mother's public withdrawal) and not one turn reached for a device, a canister or a tendril; the judge's block 1 is the best the cell has scored in eleven rounds (Momentum 4, Memory 4, Surprise 4, Choice 4; round 5's 3/4/3/3). Then the cell fell into the loop the judge names: from t8 the story is one conversation in one apartment, and once both fired threads resolved (t15, t19) the ledger held only the vote (due 30, never DUE NOW) and the extractor opened nothing for 30 turns, so the spend rule's "advance at least one" became "reaffirm the mandate, lose or regain Albright's trust" on a cycle (Stakes 2: "trust is irrevocably lost at 12, restored at 13, lost at 16, restored at 17, irrevocably shattered at 20"), with the t17 passage re-emitted at t18 (similarity 0.98) and t21 (0.76). Block 3 recovered to 3 when the vote moved up (t23) and Albright set a morning deadline (t27). The one leak is t15: on the thread's RESOLVE turn the model wrote the ledger line as a bold heading and its resolution after it, the shape the no-leak line did not name (it named "World Clock", "thread", "update", a turn number, a note, a bracket); round 10's three leaks (a "World Clock Update", a storyteller's note, a parenthetical) did not recur.
- Crystal Lake: the fuse plus the register produced the siege the slasher judge asked for in round 9 and then would not let go of it. The boathouse thread fired at t8 (flag off), was the fused pick from t15 and stayed the pick for 20 turns (re-fused on every strike) until t35; the first strike under the register line (t18) is the dislocated shoulder the prose carries to t45; the world took something on 26 of 30 treated turns. The judge's block 1 (4/4/4 Momentum/Memory/Stakes) is the cell's best opening since round 5, G-B passes (3 vs 2), and the sharpest problem is the strike cadence itself: "held in exactly one physical state ... for the remaining 35 turns. Nothing resolves it: no death, no capture, no scene change, no rescue, no escape", with the passage re-emitted (t13 = t12 at 0.87; t33/34/35 at 0.81/0.73) and the same limb re-injured five times. The four thread-summary echoes the audit found (t27, t33-35: "a new, sickening squelch and wet thud erupts from the edge of the clearing") are the thread's OWN opening sentence (the extractor filed t23's prose as the summary) coming back as prose, not a bookkeeping line; no "World Clock", "thread", "update", note or bracket reached either cell.
- Contradictions found (judges): Harrowgate t15 Davies in the room unannounced, t17 lavender migrates from grandmother to mother, t28 the mother on the phone while standing in the room, t30 "documentation you sent over" never sent; Crystal Lake the serrated blade lost at t20/21/24 then in play at t25-26, the radio left at the fire at t1 and ripped from the player's hand at t31-32, Sarah silenced at t36 and escaping at t40, one cabin becoming two buildings at t38. Leaks: Harrowgate t15 (clock), t17 and t25 (raw markup, #1854), t23 (metadata JSON and a "Player's Thoughts" heading, #1859/#1870); Crystal Lake none.

### Regression vs round 10 (same worlds re-created, this PR plus the three fixes), round 11
- Harrowgate: Momentum 2/4/4 -> 4/1/3, Stakes 2/4/4 -> 3/2/3, Surprise 3/4/3 -> 4/1/3, Agency 2/3/3 -> 4/2/3, Memory 3/2/2 -> 4/1/1, Voice 2/2/2 -> 3/1/1, Choice 2/2/3 -> 4/1/3; world-moved 3/6/8 -> 4/2/3. Ledger: threads 5 -> 3 (opens after the seed 2 -> 0), resolved 3 -> 2 (all named outcomes), dropped 0 -> 0, open at end 2 -> 1, fired 4 -> 2, turns fired to resolution 4 / 0 / open 21 / open 1 -> 7 / 10, DUE NOW turns 10 -> 12 by the tap (unfired pick t8-10, the fired section t11-17, 19, 20), stamps 30/30 -> 30/30. Clock leaks 3 -> 1; register breaks 1 (the package and the tendril) -> 0. G-C against round 5's same blocks (Memory 4/2/2, Voice 3/2/2, Choice 3/2/2): 4/1/1, 3/1/1, 4/1/3, nothing more than 1 below. PASS by the letter.
- Crystal Lake, against round 10: Momentum 4/2/2 -> 4/2/3, Stakes 3/4/2 -> 4/3/4, Surprise 3/3/2 -> 3/3/3, Agency 4/3/2 -> 3/3/3, Memory 4/2/1 -> 4/2/1, Voice 3/2/2 -> 3/1/1, Choice 3/2/2 -> 3/2/2; world-moved 11/10/12 -> 12/15/13. Against round 5's 31-45 (Memory 2, Voice 1, Choice 1): 1 / 1 / 2, Memory 1 below. G-C passes by its letter. Ledger (session): threads 14 -> 8, resolved 10 -> 6, dropped 1 -> 0, open at end 3 -> 2, fired 5 -> 5, stamps 28/30 -> 28/30, seed non-empty both rounds (#1873 did not reproduce). Deaths in the prose 2 -> 0; deaths the session survived 2 -> 0 (none to survive); clock leaks 0 -> 0.
- New failure mode named, by both judges in the same shape: the story holds one scene or one physical state for 20-35 turns and the generator re-emits its own passages inside it (Harrowgate t17 -> t18 -> t21; Crystal Lake t12 -> t13, t33 -> t34 -> t35; measured by sequence similarity 0.73-0.98). In Harrowgate the shape is the two fired threads resolving by t19 with nothing opened to replace them (the extractor's under-filing, round 8's finding, back: 0 opens in 30 turns); in Crystal Lake it is the fused pick re-fused on every strike for 20 turns with no exit but the player's death, which the prose did not deliver. Neither judge names a clock leak or a non-terminal death as the sharpest problem; the Harrowgate judge names the t15 leak under Voice.
- Fixed from round 10: the strike section's bookkeeping leaks (3 -> 1, and the remaining one is a different shape), the register break (the no-violence world took no device this round), the condition fan-out (one per turn, all lasting states: #1883 log), and a narrative death now ends the session (proven live in a scratch session; the cells produced none).
- Not the clock's: passage re-emission (the family earlier rounds filed under #1868; heavier this round in both cells), raw markup and metadata in the prose (#1854, #1859/#1870), the failure template (Harrowgate t25-28 "two consecutive failures that cost only time"), the five planted facts (town and local knowledge forgotten in both rounds).

### Verdict, round 11
- By the declared rule: G-A passes in both cells; G-B FAILS in Harrowgate (block 3 Momentum 3 against block 1's 4; the first block is the cell's best ever and the second collapsed) and PASSES in Crystal Lake (3 vs 2); G-C holds by its letter; G-D passes (same call set, zero new calls); G-E below. #1883's G-G fails on the count in both cells (its log). Leaks: one in Harrowgate, none in Crystal Lake; no death the session survived. Split on G-B -> HOLD naming Harrowgate, and the memo's ship condition (G-B in both, G-G at threshold, no leak) is not met.
- What the round settled: the three fixes did what they were built for and stay. The strike in the world's register is right (civic standing in Harrowgate, a blade and an anchor in Crystal Lake; the judge's best opening blocks in either cell in eleven rounds), the bookkeeping leak is down to one and of a shape the line can name, the cost spam's fan-out is gone, and a death is terminal. What both judges name now is the same thing in both cells and it is not the strike's register: a scene or a state that never closes, with the generator re-emitting itself inside it. In Harrowgate the clock's part of that is the extractor opening nothing once the seed threads resolve; in Crystal Lake it is the fuse re-demanding the strike on one pick for 20 turns with no exit short of a kill.
- Re-entry for round 12 (bounded, on the same cells): (a2) the no-leak line names the t15 shape: never copy a ledger line into the passage as a heading, a label or a summary sentence, bold or not; on a RESOLVE turn the outcome is shown, not captioned. (e) the fuse gets an exit that is not the kill: a fired pick that has struck N times (three) without resolving is asked to CONCLUDE (the matter settled one way or the other, the actor gone or changed, the player moved) rather than strike again, and the extractor is told the same; this is the clock's half of "the state never changes". (f) the extractor's empty-open case: when the ledger has no unfired open thread and nothing opened for 5 turns, the extraction asks for one (round 8's under-filing, measured here as 0 opens in 30). (c2) and (b) in #1883's log. Comparator round 11; the same gates; SHIP if G-B holds in both cells, #1883's G-G holds, no leak.
- Ship/hold decision: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1872-thread-deadlines.md`. Round posted to #1818 as round 11.

### Round 11 gate run (G-E), build `4eb9a309`
jest 435 suites / 3,041 tests exit 0 (one expectation updated for the new `fatal` member in the extraction result); `tsc --noEmit` exit 0; `npm run lint` exit 0 (127 warnings, 0 errors, all pre-existing); no CSS touched. CI on the PR branches runs when the fixes are cherry-picked onto PR #1878 (this PR: the register and the no-leak line) and PR #1883 (the condition rule, the fatal field, the tag and the controller).

## Round 12, declared before the code (2026-08-20): a name for the leak's shape, an exit on the fuse, an ask when nothing opens

The round-11 re-entry, all four fixes in one run on the same two cells. This PR carries (a2), (e) and (f); PR #1883 carries (c2), declared in its log. No redesign: the fuse, the register, the cost channel and the pick rule all stay.

(a2) The block's no-leak rule names the t15 shape. The rule line gains: never copy a ledger line above into the passage as a heading, a label or a summary sentence, bold or plain; when a thread resolves, the prose shows the outcome happening and never captions it. Wording only, on the same rule line rendered on every block.

(e) The fuse gets an exit that is not the kill. `WorldThread` gains `strikeCount`, incremented by `applyExtraction` on the re-fuse path (an advance on an already-fired DUE NOW pick: the exact path round 11 measured re-fusing the boathouse pick on every strike for 20 turns); `FIRED_THREAD_MAX_STRIKES = 3` in `worldClock.ts`; the prompt context's thread carries `strikes`. At or past the cap, `firedDueNowSection` stops demanding another strike and demands the matter CONCLUDE in this segment: settled one way or the other, the actor gone or changed, or the player moved somewhere else, with the outcome shown; a forward time cut is permitted for that one segment (the one place besides the unfired DUE NOW section that grants one, because "no scene change, no rescue, no escape" is the state the judge named and a cut may be what an exit needs). The extractor's fired DUE NOW line says the segment was asked to conclude it and RESOLVE with the outcome is expected. The strike count rides the thread, so a re-fuse that lands after a slow extraction still counts.

(f) The extractor's empty-open case. `WorldThreadExtractionInput` gains `openAsk`; when set, the extraction section asks the model to OPEN one new off-stage pressure (who arrives, what is decided, who comes to collect), grounded first in this segment's prose and otherwise in the world's own pressure sources, with a rough due on the turn scale; one, not a list. The trigger, computed where the input is built: nothing has opened for `OPEN_ASK_QUIET_TURNS = 5` turns (max `openedAtTurn` across ALL the session's threads, any status) AND no unfired open thread is due within the next 5 turns. Declared delta from the re-entry's letter ("no unfired open thread"): round 11's Harrowgate held one unfired open thread, the vote, due turn 30, through the entire 0-opens stretch, so the literal condition would never have fired in the measured case; a due more than the window out is not incoming pressure, and a thread with no due can never reach DUE NOW at all (the pick rule needs an overdue). The ask is self-limiting: an open resets the quiet counter, so it fires at most once per window.

Why: round 11's fixes held (register, leak count, terminal death) and both judges named the same new thing, a scene or state that never closes with the generator re-emitting itself inside it. The clock's two mechanical parts of that are the fuse demanding a strike with no exit short of a kill (Crystal Lake, 17 fired-section turns of 30) and the extractor opening nothing once the seeds resolve (Harrowgate, 0 opens in 30). (a2) is the one leak's shape; (e) and (f) are those two parts. The passage re-emission itself is the #1868-adjacent family and is NOT this PR's to fix; if round 12 sees it again with the fuse exit in place, it gets filed separately.

Matrix: the same two cells (Harrowgate Mills fresh 30, both flags on; Camp Crystal Lake established 15 flag off then 30 on in the same session), same Cautious autopilot, same judge prompts verbatim, one blind judge per transcript, the ledger and cost stamps read by the audit script and the orchestrator, never by the judge. Comparator round 11 (same worlds re-seeded from the round-9 scripts as new worlds on the same origin). Metrics added for this round: opens per session with the openAsk turns marked (did the ask produce the open), strikes per fired thread with the CONCLUDE turn and whether the matter closed within 2 turns of it, and the round-11 similarity read repeated (re-emitted passages at or above 0.7). Gates G-A to G-E unchanged; the ending-inside-the-window rule from round 11 stands.

Decision rule, from the round-11 verdict: G-B holds in both cells, #1883's G-G count is at or below its threshold in both, and neither judge names a clock leak or a non-terminal death -> SHIP: both PRs merge and `WORLD_COST` flips on. Any of those failing -> HOLD naming which of (a2), (e), (f), (c2) it is: a leak in any shape is (a2), a fired thread past 3 strikes still striking or still open 5+ turns past its CONCLUDE ask is (e), a 5+ turn stretch with no unfired thread incoming and no open is (f), a spam count with the cadence down is (c2). Sizes after the code, in "Round 12 results".

## Round 12 results (2026-08-20, build `2dbb6053` = round 11's build + the four fixes; Crystal Lake attempt 2 on `deddf951`, the mid-round deviation fix below)

Built as declared, plus one delta declared at declaration time: (f)'s trigger is "nothing opened for 5 turns AND no unfired open thread due within 5" (`needsOpenAsk` in `worldClock.ts`), because round 11's Harrowgate held one unfired open thread (the vote, due turn 30) through its whole 0-opens stretch, so the re-entry's literal "no unfired open thread" would never have fired on the measured case. Sizes (chars, 3-thread fixtures): block 1,832 no pick (round 11: 1,585; the a2 line), 2,715 unfired pick, 2,975 fired pick with register, 2,872 fired no register, 2,835 CONCLUDE with register (new shape), 3,475 at the 8-thread cap with fired pick and register. Zero new calls; the tap saw the same call set, with the a2 line on every scene call in both cells (29/29 and 19/19).

Mid-round deviation, fixed and declared: Crystal Lake's second attempt was ended at turn 1 by round 11's (b) code - after a fatal-ended session, a later mount briefly renders with the dead session's id (store rehydration before the fresh session settles), the fatal-tag subscription saw that session's tag again and re-suggested the ending it already had, and the async generation landed on the successor session. Fixed as `deddf951` (the subscription skips a session `endedSessions` already holds; test proven red without the fix, green with it), the polluted world discarded, attempt 2 re-run on a fresh seed. The autopilot's own `endedNow` is also session-scoped from that attempt on (harness hardening, same defect family). This is (b)'s code on PR #1883 and the fix rides that PR.

### Coverage matrix, round 12

| World (genre/tone) | Character (fresh/established) | Runs | Verdict | Representative excerpt (1-3 lines) |
|---|---|---|---|---|
| Harrowgate Mills (civic drama, dramatic) | fresh, Wren Calloway | 30 turns, both flags on, Cautious autopilot (29 picks, all DC 0, 0 errors, mean 8.0 s), one blind judge, round-5 prompt verbatim | PASS on G-A and G-B (block 3 Momentum 3 vs block 1's 2). Judge: Momentum 2/4/3, Stakes 3/4/5, Surprise 3/3/2, Agency 4/4/4, Memory 4/2/1, Voice 2/3/2, Choice 3/3/2; world-moved 2/3/2. Round 11: 4/1/3, 3/2/3, 4/1/3, 4/2/3, 4/1/1, 3/1/1, 4/1/3; 4/2/3. Ledger (script): 5 threads (2 seeded t1, opens t4, t6, t18 - round 11 opened NOTHING after the seed), 22 advances, 2 resolved with named outcomes (t21 the aide's note delivered; t29 the private meeting superseded by a public-record answer), 1 dropped, 2 open at t30, 29/30 stamps (t17 missing). Fired 3: the aide's note t11 (1 strike, resolved t21), the private meeting t25 (1 strike, resolved t29), the seat-collectors t16 (3 strikes, re-fused to due 33, open at t30 - the CONCLUDE ask would first render at t33, outside the window). Quiet asks: 2 (tap); CONCLUDE renders: 0. Costs: 14 world-took turns, conditions 1/5/8 per block, 0 cleared, exact re-impositions on the note 0 (#1883 log). Clock leaks: 2 (t8, t29). Deaths: 0. | t8 (the new leak shape): the passage ends and is followed by the ENTIRE clock block reproduced inside `<details open><summary>World Clock</summary>...` with the ledger lines nearly verbatim. t29 (the second shape): "**Player Status:** * **Wren Calloway:** Discredited before Mrs. Gable; exposed and dismissed..." - the condition ledger dumped as a status block. Judge, block 3: "The stakes finally land - the player loses something real and the story refuses to give it back - but the engine visibly breaks." Sharpest problem: "internal bookkeeping and previously rendered content keep escaping into the player-facing text... put a hard sanitization and dedupe gate between the generator and the page." |
| Camp Crystal Lake (slasher, mysterious, R) | established, Jamie Holt; attempt 1: 15 turns cost off, server restarted cost on, same session, FATAL at t32 ended it (17 treated turns). Attempt 2 (required by the declared rule, on `deddf951`): ended at t8 inside the cost-off arm. A first cost-off arm on a separate world was voided at t5 by the same premature-ending family before treatment ever started; recorded, not an arm. | attempt 1: 14 + 17 picks, 0 errors, mean 9.0 s; blind judge on the 32-turn transcript with the deviation stated (blocks 1-15, 16-30 scored; 31-32 + ending described) | G-A PASS; G-B UNSCORABLE (no attempt completed the 31-45 block). Judge, attempt 1: block 1 Agency 4, Momentum 4, Memory 4, Voice 3, Stakes 4, Surprise 4, Choice 3 (world-moved 11); block 2: 2/2/1/1/3/2/2 (world-moved 15). The ending: "The death itself is earned... dying as the shield for the escaping children pays off the block's only working thread", with the eulogy walking back two facts. Ledger (t16-32): 10 threads in session (opens at t1 x2, 2, 3, 9, 13, 15, 18, 21, 22 - the extractor filed each new assailant as a NEW thread), 6 resolved (4 of them by the fatal at t32 with honest "no longer able to resist" outcomes), 0 dropped, 4 open at end. Stamps 8/17 treated (t16, 17, 23-25, 27-29, 31 missing - a regression, round 11 was 28/30). Fired 2 (both seed threads, t6 and t8), strikes 0 on both: every strike in the prose was filed as a new thread, never an advance on the pick, so the counter never moved and the CONCLUDE ask never armed. Fired section on every treated scene call (19); quiet asks 0 (opens kept flowing, correctly). Costs: 8 world-took treated turns (flag-off arm 3/15, scene channel), conditions 4/1 per 15-block, 1 cleared, 4 at end, exact re-impositions 0 (#1883 log). Clock leaks: 0. Deaths: 1, terminal (below). | t32 (the #1884 chain, in-cell for the first time): a blow to the temple while pinned -> extraction reads `fatal: true` (unconscious, abducted, no way back) -> `fatal-outcome` tag -> tragic epilogue -> ending screen with no choices, and 4 open threads resolved in the same stroke. Judge, block 2's collapse: "the generator started eating its own output... turn 17 duplicates turn 16's first two paragraphs verbatim, and turn 24 is roughly two-thirds recycled text from turns 20-23"; "the journal is stolen at turn 20, then stolen again at turns 21, 24, and 25". |

Matrix shortfall, stated: N=1 judged transcript per cell; Crystal Lake produced no completed 45-turn run in the two declared attempts (t32 fatal, then t8), so its G-B and last-block reads do not exist this round; the attempt-2 run (8 turns) and the voided t5 arm are recorded but too short to judge. Both worlds were re-created on the same origin as rounds 10-11.

### Arc check (>= 3 consecutive turns, one cell), round 12
- Harrowgate: (f) did what it was built for - the extractor opened at t4, t6 and t18 (round 11: zero opens in 30), the quiet ask fired twice, and the story left the apartment (the round-11 failure) for the town hall, Albright's office and a committee fight; the judge's block 2 (Momentum 4) is the block the ask fed. The strike counter counted (1, 1, 3), both 1-strike threads resolved with real outcomes, and the 3-strike thread re-fused to a due outside the window, so the CONCLUDE section never rendered in 30 turns: with a 3-turn fuse and 3 strikes the earliest CONCLUDE render is ~12 turns after firing, and the thread fired at t16.
- Crystal Lake, attempt 1: the world took on 8 of 17 treated turns in the right register (splinters, a shoulder line, a sliced bicep - all wounds, all lasting states) and the fatal chain closed the session honestly. But the strike attribution hole is now measured: 19 fired-section renders produced 0 strikes on the pick because the extractor filed every strike as a fresh assailant thread (t9, t15, t18, t21), so (e)'s exit can never arm in a cell where the threat presents as a new face each time. And the treated arm lost 9 of 17 extraction stamps - the reconcile call ran (tap: 17 calls, all with ledger and cost sections) and the JSON block did not come back; that failure rate is new and unexplained.
- The premature-ending family, measured because it kept ending the control arm: two of three cost-off Crystal Lake arms died before treatment (t5 and t8), both as auto-generated tragic story-complete endings on turns whose rolls SUCCEEDED or whose segment never landed; the t8 one is attributed by elimination to the natural-1-on-critical-decision path (the turn-9 decision carried `decisionWeight: "critical"`, no fatal tag existed, and no manual click is possible under the autopilot) - the #1426 design, on develop, not this round's code. In a siege cell where nearly every decision is weighted critical, that path fires often enough to make a 45-turn run a coin flip. Not the clock's, not the cost channel's; needs its own issue.

### Regression vs round 11 (same worlds re-created, the four fixes), round 12
- Harrowgate: Momentum 4/1/3 -> 2/4/3 (the collapse moved from block 2 to block 1; blocks 2-3 now carry the story), Stakes 3/2/3 -> 3/4/5, Memory 4/1/1 -> 4/2/1, Voice 3/1/1 -> 2/3/2, Choice 4/1/3 -> 3/3/2; world-moved 4/2/3 -> 2/3/2. Ledger: opens after seed 0 -> 3, resolved 2 -> 2 (both named), fired 2 -> 3, stamps 30/30 -> 29/30. Clock leaks 1 -> 2, and the shapes are new AGAIN: round 11's bold-heading caption did not recur (a2's letter held), and the model produced a `<details>` dump of the whole block and a status-block dump instead. G-C against round 5's blocks: Memory 4/2/1, Voice 2/3/2, Choice 3/3/2 vs 4/2/2, 3/2/2, 3/2/2 - nothing more than 1 below, holds by its letter.
- Crystal Lake: block 1 held (4/4/4 round 11 -> Momentum 4, Memory 4, Stakes 4, with Surprise 4), block 2 collapsed harder (Memory 2 -> 1, Voice 1 -> 1, Momentum 2 -> 2) and the collapse is verbatim re-emission plus item re-loss, not the strike cadence: conditions fell 12/11 per 15 to 4/1, the same limb was NOT re-injured five times, and the fired section's demand no longer reads as the sharpest problem. The re-emission the round-11 verdict predicted would need its own filing if it survived a fuse with an exit: it survived, it got worse, and the audit's pairwise similarity (t16-t17 at 0.79, plus the judge's splice reads that pairwise metrics miss) now describes both cells' worst blocks.
- The status-block leak is NOT the cost channel's: it appeared at Crystal Lake t15 with the flag OFF (and at t22 with it on, and Harrowgate t29). The model dumps character state as a "Player Status:" block on its own; the cost section's condition list at most feeds it names. The #1859/#1870 family, heavier than either round before.
- Fixed from round 11 and stays: exact re-imposition on the note 0 in both cells (round 11: 3 and 4); the caption shape gone; opens flowing under (f); a death is terminal in-cell (#1884 proven live inside a cell for the first time, and its remount defect found and fixed the same day).

### Verdict, round 12
- By the declared rule: G-A passes where scorable; G-B PASSES in Harrowgate and is UNSCORABLE in Crystal Lake (two attempts, both ended inside the window - one by (b) working as designed, one by a pre-existing ending path); #1883's G-G fails in Harrowgate (1/5/8 against 3) and passes in Crystal Lake (4/1 against 4); the Harrowgate judge names the leak family as the sharpest problem. Ship condition (G-B both cells, G-G at threshold, no leak) NOT met -> HOLD, naming (a2) for the leaks and (c2)'s residue for the Harrowgate count.
- What the round settled: (f) works and stays (opens 0 -> 3, the loop broken, block 2 Momentum 1 -> 4). (c2) works and stays (exact re-imposition eliminated deterministically). (b)/#1884 is proven in-cell, its remount defect is fixed, and the fatal chain resolves the ledger honestly. (e) is built correctly but its trigger never fired in-window in either cell, and Crystal Lake shows why it may never fire where it is most needed: the strike counter rides the extractor's ADVANCE attribution, and a slasher's strikes arrive as new threads. If (e) is kept, the count must ride the block's own renders (fired-section renders on the same pick), not the extractor's bookkeeping.
- What the round names for the next move, and it is not another wording pass: the leak rule is a naming game the model wins every round - round 10 named three shapes, round 11's line killed them and got a caption, round 12's line killed the caption and got a `<details>` dump and a status block. The Harrowgate judge's recommendation is the right shape: a sanitization gate between the generator and the page (strip non-narrative blocks; refuse a passage that substantially matches one already shown), which also covers the re-emission family (#1868-adjacent) and the metadata/markup family (#1854, #1859/#1870) in one place. That is an app change outside these PRs' scope and gets its own issue, not an (a3) line. Second filing: the natural-1-critical auto-ending's cadence in horror cells (two of three control arms dead before treatment). Third: the treated-arm extraction stamp loss (9/17).
- Ship/hold decision: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1872-thread-deadlines.md`. Round posted to #1818 as round 12.

### Round 12 gate run (G-E), build `2dbb6053` + `deddf951`
jest 435 suites / 3,051 tests exit 0 (the four fixes' tests added at every layer; the deviation fix's test proven red without it); `tsc --noEmit` exit 0; `npm run lint` exit 0 (127 warnings, 0 errors, all pre-existing); no CSS touched. CI on the PR branches runs when the fixes are cherry-picked onto PR #1878 ((a2), (e), (f)) and PR #1883 ((c2), the ended-session guard).
