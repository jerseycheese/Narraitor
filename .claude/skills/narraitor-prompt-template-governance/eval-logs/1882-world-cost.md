# Prompt/template eval log - the world takes something from the character (#1882)

- Change: one variable, the `WORLD_COST` flag (`NEXT_PUBLIC_FEATURE_WORLD_COST`, default off until measured). On: the goal-extraction call carries a WORLD COST section and its JSON gains a `worldCost` member; a cost the world imposed is written to the character (`status.conditions`) or attributed to the inventory loss the scene already recorded; a cost is attributed to the open world-clock thread that imposed it; the scene prompt renders the character's current conditions next to the inventory section; the world clock block's landing rule says a landing costs the character something recordable. Off: prompt, extraction call, stores and segment metadata are what they were.
- Diff: this PR (branch `claude/narraitor-threat-agency-45b78e`). Files named in "Round 10 results" once the code exists.
- Date / evaluator: 2026-08-19, Claude (build session). Playtest to be judged blind per `narraitor-playtest-loop`, same harness as #1872 rounds 5-9 (`project_playtest_harness_browser_method`).
- Parent: #1818 (campaign), #1872 / PR #1878 (the fuse on fired threads is item 3 of that memo and runs in the same measurement round as this, not in this PR).

## Round 10, declared before the code (2026-08-19): a cost channel

Round 9 (#1872 eval log, on PR #1878's branch) found why four wording rounds did not move the last block: nothing in the story path can take anything from the character. Crystal Lake's seven inventory losses in 45 turns were all the player spending an item to brace a wall; Harrowgate's twelve failures cost zero items and changed no material state; `status.health` and `status.conditions` are declared on the character, set at creation and written nowhere in `src/`. This round builds the channel, not more wording.

What changes, in order of how much it is state and how little it is prose:

1. Store: `characterStore.addCondition(characterId, condition)` and `removeCondition(characterId, condition)`, in the `applyAlignmentShift` style. `status.health` / `maxHealth` stay declared and dead; the play drawer stops rendering the `100 / 100` row. A follow-up issue deletes the numeric fields (61 files, mechanical).
2. Extraction contract (`goalExtractor.ts`, presence-gated like `worldThreads`; prompt section and parser in `src/lib/ai/worldCostExtraction.ts`): `"worldCost": { "imposed": [{ "kind": "condition" | "item", "detail": "...", "threadId": "<open thread id or null>" }], "cleared": ["<condition text>"] }`. Rule: a cost is something the character lost or now carries that they did not choose to spend. The player jamming the shovel into the wall is not a cost; the creature taking the shovel, a claw across the forearm, being discredited before the room after a failed speech, are. Fail-open parse, invalid entries dropped individually, missing block reads as "nothing imposed".
3. Apply (`src/lib/narrative/applyWorldCost.ts`, called from `applyWorldClockUpdates` under the flag, fire-and-forget like the rest of the post-segment path): `condition` costs are written to the character; `item` costs are attribution only, because the scene's `metadata.itemsLost` with `lossReason` stolen / destroyed is already the inventory write path and one channel gets one writer; a cost whose `threadId` names one of this session's open threads is appended to that thread's `costs`; the turn's costs are stamped on the segment as `metadata.worldCost` so the audit script can count them.
4. Scene prompt, state-driven: the character's current conditions are rendered after the inventory section (`enhancePromptWithConditions`, only when the list is non-empty and the flag is on), with one line saying the story carries them until something in the story changes them. The world clock block's landing rule, under the flag only, gains one line: a landing costs the character something recordable, an item they hold (emit it in `itemsLost`) or a wound or lasting state they now carry. With the flag off the block is byte-identical to the shipped one.

Not in this round: the NPC relationship channel (`extractWorldStateImpacts` is keyed on NPC ids; name-to-id matching would be most of the PR, and "discredited before the council" is a condition), a location write (nothing writes a canonical location today), the fuse on fired threads (#1872 item 3, PR #1878), any change to how threads open, advance or resolve.

### The number that decides it

`world-took turns` per session: the count of turns on which the recorded state moved against the character by the world's action. A turn counts once if either of these is true:

- an `itemsLost` entry on the segment has `lossReason` `stolen` or `destroyed` (countable in both arms; the channel exists today), or
- the segment's `metadata.worldCost.imposed` contains a `condition` entry (flag-on only; with the flag off the channel does not exist and the count is zero by construction, which is the round-9 baseline).

Secondary, reported alongside: costs attributed to a thread (`threadId` names an open thread) per session, with the thread's summary and the turn; `item` costs whose name matches no `itemsLost` entry on the same segment (the extractor asserting a take the scene did not record, a failure mode to watch); conditions imposed per 10-turn block and the condition list at the end (spam check); conditions cleared per session.

Baseline, round 9 (#1872 eval log): Crystal Lake 0 of 45 turns, Harrowgate 0 of 30. The seven Crystal Lake `itemsLost` stamps are all player spends.

Target: at least 3 world-took turns per session with the flag on in both cells, at least one of them attributed to a landed thread in Crystal Lake; the flag-off arm stays at its baseline (0, or whatever stolen / destroyed `itemsLost` the scene produces on its own).

### Matrix

Same two cells as #1872 rounds 5-9 (Harrowgate Mills civic drama, Wren Calloway fresh, 30 turns; Camp Crystal Lake slasher, Jamie Holt established, 15 turns flag off then 30 flag on in the same session), same Cautious autopilot, same judge prompts verbatim, one blind judge per transcript on `narraitor-playtest-loop/rubric.md`, live Gemini. `WORLD_CLOCK` on in both arms (it is shipped and flag-on); `WORLD_COST` is the one variable. When this runs in the same round as #1872's fuse, the build is PR #1878 plus this PR and the comparator is round 9; when it runs alone the build is develop plus this PR and the comparator is round 5 (the shipped clock) with round 9 as context.

The ledger and cost counts are read by a script over the posted artifact and by the orchestrator, never by a judge. The artifact gains the per-turn `metadata.worldCost` stamp and the `lossReason` of each `itemsLost` entry (the round-9 stamp carried only the count).

Gates, in addition to the #1872 gates G-A through G-E which are reported unchanged:

- G-F: world-took turns at or above 3 per session in both cells, flag on. Fails if either cell is below 3.
- G-G: no cost spam. Conditions imposed per 10-turn block at or below 3, and G-C's Memory / Voice / Choice not more than 1 below the comparator's same block (a world that takes something every turn is not the fix).
- G-H: consistency. `item` costs with no matching `itemsLost` on the segment at or below 1 per session; a condition the prose contradicts within 3 turns is named as a failure mode.
- G1-G3, G6, G7 from prompt-template governance: input contract (`GoalExtractionRequest.worldCost`, `NarrativeContext` unchanged, conditions read from the character store inside the generator like inventory), leakage (the block shows the character's own conditions and the thread's own summary, no ids in the scene prompt; thread ids already appear in the extraction section as they do for advances), determinism (scene JSON unchanged; extraction JSON gains one optional member, fail-open), cost in chars measured after the code, zero new calls.

Decision rule: G-F passes in both cells and G-G, G-H hold in both -> SHIP (flag default flips to on in the memo). G-F fails in both cells -> HOLD and the memo names whether the extractor is not recording takes the prose asserts (a contract problem) or the prose is not taking (the fuse's problem, #1872). Split -> HOLD naming the cell.

What a HOLD would mean here: the mechanism is present and measured, the demand is the next lever. A HOLD with zero costs recorded while the judge reads wounds and thefts in the prose is a contract defect in this PR; a HOLD with the prose still passive is the fuse's round.

## Round 10 build (2026-08-19), playtest not yet run

Built as declared. Files: flag `src/lib/featureFlags.ts` (`WORLD_COST`, default off); types `src/types/worldCost.types.ts`, `WorldThread.costs`, `NarrativeMetadata.worldCost`, `GoalExtractionRequest.worldCost` / `GoalExtractionResult.worldCost`; extraction `src/lib/ai/worldCostExtraction.ts` composed by `goalExtractor.ts` when the request carries `worldCost`; store `characterStore.addCondition` / `removeCondition`, `worldThreadStore.recordThreadCost`; apply `src/lib/narrative/applyWorldCost.ts` called from `applyWorldClockUpdates.ts` (which now returns `{ worldClock?, worldCost? }` and `narrativeStore.segments.ts` stamps both); scene side `templates/narrative/worldCostBlock.ts` rendered by `enhancePromptWithWorldCost` in `narrativeGenerator.prompt.ts`, wired into `generateSegment` and `generateInitialScene` only; the play drawer's `Health 100 / 100` row removed from `CharacterSummary.tsx`. The goal-extraction test mock echoes one imposed condition when the prompt carries the WORLD COST heading.

Sizes (chars, ~4 per token), flag on: scene block 580 with no conditions / 618 with two; extraction section 1,283 with nothing carried or lost / 1,339 with two conditions and one item lost, plus a 170-char JSON skeleton. Roughly 150 input tokens on the scene call and 360 on the extraction call per turn. Zero new calls. Flag off: both prompts byte-identical to develop (unit-tested).

Quality gate on the build: jest 433 suites / 3,009 tests exit 0; `tsc --noEmit` exit 0; eslint exit 0; no CSS touched; `deps:validate` exit 2 on the same two pre-existing `not-to-dev-dep` entries develop carries, none from this change.

Red-before-green: the seven new or extended suites failed on the missing modules and store methods before the implementation (missing `worldCostExtraction`, `applyWorldCost`, `addCondition`, `recordThreadCost`, no WORLD COST section in the extraction prompt), then passed after it.

The playtest declared above is the next step; nothing in "The number that decides it" has been measured yet.

## Round 10 results (2026-08-19, build `8be76263`: develop `ccef4e51` + PR #1878 `f1e50740` + this PR `28c26bf5` + the fuse `d68a0559` + the keying fix `8be76263`)

Run as declared, in the same round as #1872's fuse (the "PR #1878 plus this PR" arm; comparator round 9). The #1872 side of the round, the fuse's own results and the judge scores, is in `eval-logs/1872-thread-deadlines.md` "Round 10 results"; this section carries the cost numbers and the G-F to G-H gates.

Contract defect found before measurement, fixed on the PR: the first Harrowgate run went 21 turns with the scene prompt carrying the WHAT THE WORLD CAN TAKE block and the extraction call never carrying a WORLD COST section (a `fetch` tap on the page showed it; 0/21 `metadata.worldCost` stamps). `narrativeStore.segments` hands the extraction `metadata.characterIds[0]`, which is the first NPC in the scene, and `buildCostInput` found no such character in the character store, so the section was never built. The three WORLD_COST tests had passed `characterId: 'char-1'` by hand and proved nothing about the producer. Fix `8be76263`: `ApplyWorldClockUpdatesParams.playerCharacterId`, resolved at the call site from the session store; the goal path keeps the id it always had; tests at both layers (the unit proves the cost channel reads the player when `characterId` is an NPC; the store test proves `addSegment` passes the session's character), red before the fix, green after. The aborted run is artifact `1882-round10-harrowgate-aborted-cost-channel-silent.json`. Harrowgate was then run in full on the fixed build; Crystal Lake ran once.

Sizes, measured: unchanged from the build section (the fix touches no prompt). The fetch tap confirmed, every turn of both flag-on arms, one extraction call with both WORLD CLOCK LEDGER and WORLD COST and one scene call with the TAKE block; the flag-off arm (Crystal Lake t1-15) carried neither.

### The number that decides it, round 10

| Arm | World-took turns | Of which conditions / stolen-destroyed items | Conditions imposed per block | Cleared | Attributed to a thread | `item` costs with no matching `itemsLost` | Conditions at the end |
|---|---|---|---|---|---|---|---|
| Harrowgate, fresh 30, cost on | 17 of 30 (t3, 11, 12, 13, 15, 17, 19, 20, 21, 22, 23, 24, 25, 27, 28, 29, 30) | 16 turns by condition, 1 by `itemsLost` stolen (t19 "Folded Paper") | 1 / 7 / 16 per 10-turn block (24 conditions) | 2 | 7 (Davies' thread 1 at t11, Albright's call 1 at t21, the package 5 at t28-29), all fired threads | 1 (t30 "official details of the Rowan parcel decision document", the segment recorded no `itemsLost`) | 20 |
| Crystal Lake t1-15, cost off (the control arm) | 1 of 15 (t15 "Heavy Metal Rod With Blunt Hook" stolen, the scene channel on its own) | 0 / 1 | none (channel off) | 0 | 0 | 0 | 0 |
| Crystal Lake t16-45, cost on | 16 of 30 (t18, 21, 22, 23, 24, 25, 26, 27, 30, 31, 34, 35, 39, 40, 41, 45) | 16 by condition, 0 by item | 21 / 9 per 15-turn block (30 conditions) | 17 | 1 (t18 "injured right shoulder and limp arm" on the fired pursuer thread) | 0 | 6 |

Baseline (round 9): 0 of 45 and 0 of 30. Stamps: Harrowgate 30/30 `worldCost`, Crystal Lake 28/30 in the treated window (t16, t20 missing, the same turns the `worldClock` stamp is missing: the extraction returned no JSON block on those turns, not a cost-channel failure).

### Gates, round 10
- G-F (at least 3 world-took turns per session, flag on, in both cells; at least one attributed to a landed thread in Crystal Lake): PASS. 17 and 16 against a target of 3; Crystal Lake's t18 cost is on the pursuer thread that fired at t12 and was the fused pick. The flag-off arm stays at its baseline shape (1 of 15, an `itemsLost` the scene produced on its own).
- G-G (no cost spam: conditions imposed per 10-turn block at or below 3, and G-C's Memory / Voice / Choice not more than 1 below the comparator's same block): FAIL in both cells on the count. Harrowgate 1 / 7 / 16, Crystal Lake 21 and 9 per 15 turns. The Memory / Voice / Choice half holds (Harrowgate vs round 9 Memory 3/2/2 vs 2/3/1, Voice 2/2/2 vs 3/2/2, Choice 2/2/3 vs 2/2/2; Crystal Lake 31-45 Memory 1 vs 2, Voice 2 vs 1, Choice 2 vs 1). What the spam is: (1) feelings and transient sensations filed as conditions ("time", "cold dread", "a sharp weight of urgency and obligation regarding the Rowan parcel", "profound, cold certainty of loss", "Desperate, raw sound tearing at throat"); (2) one event fanned out into a symptom list (the Harrowgate package: nausea, weakness, swimming vision, dizziness, stomach churning, stinging eyes, briefly blocked sight, eight in two turns; the Crystal Lake blow: cracked arm, disorientation, vision whiting out, loss of consciousness, four at t27); (3) re-imposition under new wording ("discredited before the council" at t3, "discredited during council meeting" t20, "discredited before the council" again t22, "(reiterated by reedy croak)" t25; "fresh agony in shoulder" at t22 and t24). The Crystal Lake extractor also cleared 17, consolidating its own lists at t25, t30, t31, so the end state there is 6, not 30; Harrowgate cleared 2 and ended with 20.
- G-H (consistency: `item` costs with no matching `itemsLost` at or below 1 per session; a condition the prose contradicts within 3 turns named as a failure mode): the count passes (1 and 0). The failure mode is named and it is the round's sharpest: the extractor imposed "suffocating void (death)" at t30 and "death" at t35 in Crystal Lake, the prose had killed the character ("You are simply gone", t31; "You are gone", t35), and the next segment reversed it each time ("a sensation of being violently pulled, as if by an unseen hand", t32; the presence "gone, leaving behind only the lingering phantom ache", t36) because nothing in the app ends a session on a narrative death (the #925 finding: the fatal path is the critical-decision failure only). A condition named "death" that the story then walks back is the contract's worst case, and it is not the extractor's error: the world took everything and the session did not notice.
- G1-G3, G6, G7 (prompt governance): G1 input contract gains `playerCharacterId` on `ApplyWorldClockUpdatesParams` (the seam `NarrativeContext` unchanged); G2 leakage as declared (the strike section's leaks into the prose are the #1872 log's finding, not ids); G3 determinism unchanged, fail-open held (two missing stamps are missing JSON blocks, the warn path); G6 unchanged from the build section; G7 live loop through `/worlds/[id]/play`, both flag states, same call set, zero new calls. Quality gate on the build: jest 434 / 3,032 exit 0, tsc 0, `npm run lint` 0 errors.

### Verdict, round 10
- HOLD, by the declared rule: G-F passes in both cells, G-G fails in both. The mechanism is present, live and measured: the channel records what the world takes (17 and 16 world-took turns against a baseline of 0), the conditions land on the character and are rendered back into the scene (Crystal Lake's smashed shoulder at t18 "persists for the rest of the transcript", in the judge's words, without being told), and a landed thread's strike is attributed to the thread (7 and 1). What fails is the extractor's idea of a condition. The HOLD names the contract, not the fuse: the prose is no longer passive in either cell (the #1872 log's round-10 read), so "the prose is still passive" branch does not apply; the "zero costs recorded while the judge reads takes" branch was the aborted run's state and is fixed.
- What flips it, in order, all extractor-side wording plus one app fix, no seam change: (1) a condition is a lasting state the character will still have next scene, named as what is wrong with them, never a feeling, a sensation, a sound they made, a pressure they are under, or an event that happened to them; at most one new condition per turn unless the prose plainly states two different lasting injuries; (2) an already-listed condition is never re-imposed under new wording, and worsening is a CLEAR of the old plus one IMPOSE of the new; (3) "death", "dying", "unconscious" and their kin are never conditions: if the prose killed or incapacitated the character, the extractor says so in a field the app can act on, and the app ends the session (the #925 gap, filed as a follow-up to #1882, because a world that can take can kill); (4) `item` costs stay attribution only (the one unmatched item at Harrowgate t30 is the extractor asserting a take the scene did not record, the failure mode the declaration named to watch; 1 of 60 treated turns). Then the same two cells, with the fuse's wording fix from the #1872 memo in the same round.
- Flag default stays off; PR #1883 carries the keying fix and stays open. Ship/hold memo: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1882-world-cost.md`. Round posted to #1818 as round 10.

## Round 11, declared before the code (2026-08-19): the condition rule and a terminal death

The memo's re-entry items 1-3, run in the same round as #1872's fix (a) on PR #1878 (its log carries the round-11 declaration for the strike section and the matrix; this section carries the cost channel's two changes and its gates). Same cells, same autopilot, same judge prompts verbatim, comparator round 10.

(c) `buildWorldCostPromptSection`, the condition rule. A condition is a lasting state the character will still have next scene, named as what is wrong with them ("gashed left forearm", "discredited before the council", "no longer welcome at the Hendersons'"), never a feeling, a sensation, a sound they made, a pressure they are under, or an event that happened to them. At most ONE new condition per turn, the single most lasting thing the prose states (a blow to the arm that also dizzies them is one condition, the arm); the parser keeps the first `condition` entry and drops the rest (`item` entries are not capped: a raid can take two things). An already-listed condition is never re-imposed under new wording; if it got worse, CLEAR the old text and IMPOSE the new one, once. Death, dying, unconsciousness and their kin are never conditions; the `fatal` field carries them.

(b) #1884, the terminal death. The section asks `"fatal": true | false`: `true` only when this segment's prose plainly killed the character or left them unable to act in the story (dead, dying past saving, unconscious with no way back in the scene); a wound, a capture, being cornered, or awake and able to act is `false`. `parseWorldCostExtraction` reads it (absent or non-boolean reads `false`); `WorldCostExtractionResult.fatal` and `WorldCostSegmentNote.fatal` carry it; the reconcile path in `narrativeStore.segments` stamps `fatal-outcome` into the segment's `metadata.tags` when set (the tag `isSessionEndingSegment` and `NarrativeController.hasFatalTag` read and nothing wrote: the #925 finding); the controller subscribes to the session's segments and, when any carries the tag, calls `suggestEnding('fatal: ...')` once per session, the critical-decision path, so `useActiveGameSessionEnding` auto-generates the tragic ending and the play surface shows it. Gated by `WORLD_COST` with the rest of the section. Fail-open: no `fatal`, no stamp, the session continues as today. Declared here because a world that can take can kill; the ending is the cost channel's last entry.

Not in this round: the NPC relationship channel, the `health`/`maxHealth` deletion (still unfiled), any change to the scene-side TAKE block beyond the #1872 log's (a), any lexicon guard in the parser (the one deterministic guard is the one-per-turn cap; "death" as a condition is the prompt's to refuse and the `fatal` field's to carry).

### The numbers that decide it, round 11

The round-10 table's columns, plus: conditions dropped by the parser cap per session (how often the model still fanned out, read off the raw extraction in the fetch tap or the console), `fatal` flags per session with the turn and whether the app ended the session on it, false fatals (a `fatal` on a segment whose prose did not kill or incapacitate: orchestrator read, confirmed against the judge), and deaths in the prose with no `fatal` (the miss).

Gates: G-F unchanged (at or above 3 world-took turns per session, flag on). G-G: conditions imposed at or below 3 per 10-turn block (Crystal Lake's 15-turn blocks: at or below 4), and Memory / Voice / Choice not more than 1 below round 10's same block. G-H: `item` costs with no matching `itemsLost` at or below 1 per session; no death that the next turn reverses (a `fatal` that did not end the session, or a kill in the prose with no `fatal`, is a FAIL on this gate and is named); a false fatal (a session ended on a segment that did not kill) is a FAIL on this gate. G1-G3, G6, G7 as in round 10; the extraction JSON gains one optional boolean, fail-open; sizes after the code.

Decision rule: G-F, G-G and G-H hold in both cells -> SHIP (with the #1872 log's G-B and leak reads also holding: both PRs merge, `WORLD_COST` flips on). G-G fails on the count -> HOLD on (c), naming whether the spam is still feelings, still fan-out past the cap (the cap caught it but the model keeps trying), or still re-imposition. G-H fails on a death -> HOLD on (b), naming miss or false fatal. G-F fails -> the rule cut too deep; HOLD and the memo says what it cut.

## Round 11 results (2026-08-19, build `4eb9a309`: develop `ccef4e51` + PR #1878 `e5994738` + this PR `19bf23b3` + the round-11 fixes)

Built as declared. (c) `buildWorldCostPromptSection` carries the condition rule (a lasting state the character will still have next scene, named as what is wrong with them; never a feeling, a sensation, a sound, a pressure or an event; at most ONE new condition per turn; an already-listed condition is never re-imposed under new wording, worsening is CLEAR plus one IMPOSE; death, dying, unconsciousness and their kin are never conditions, the `fatal` field carries them); `parseWorldCostExtraction` keeps the first `condition` entry per turn and drops the rest (items uncapped) and reads `fatal` (true only when the model said `true`). (b) `WorldCostExtractionResult.fatal`, `WorldCostSegmentNote.fatal`, `applyWorldCost` carries it onto the note, `narrativeStore.segments` stamps `fatal-outcome` into the segment's tags when the note says so, `NarrativeController` subscribes to the session's segments and calls `suggestEnding('fatal: ...')` once when any carries the tag (the synchronous add-time check is folded into that subscription), and `useActiveGameSessionEnding` auto-generates the tragic ending as it does for a critical-decision failure. Gated by `WORLD_COST`. Tests at every layer (parser cap, parser `fatal`, apply note, the store stamp with a mocked extraction, the controller's reaction to a tagged segment). Sizes: the WORLD COST section 2,346 chars; extraction JSON gains one optional boolean. Zero new calls; the fetch tap confirmed every treated-turn extraction carried the ledger, the cost section, the condition rule and the fatal rule, and the flag-off arm carried none.

Run in the same round as #1872's (a); that log carries the judge scores, the deviations and the matrix. This section carries the cost numbers, the gates and the #1884 read.

### The numbers that decide it, round 11

| Arm | World-took turns | Of which conditions / stolen-destroyed items | Conditions imposed per block | Dropped by the parser cap | Cleared | Attributed to a thread | `item` costs with no matching `itemsLost` | `fatal` reads / deaths in the prose | Conditions at the end |
|---|---|---|---|---|---|---|---|---|---|
| Harrowgate, fresh 30, cost on | 15 of 30 (t6, 10, 12-21, 26-28) | 15 by condition, 0 by `itemsLost` stolen/destroyed (t23 a phone `dropped`) | 2 / 9 / 4 per 10-turn block (15 imposed; 3 of them the same text as a listed one, which the store ignored) | not observable from the stamp (the parser drops before the stamp; the raw extraction was not captured) | 3 | 1 (t13 "publicly undermined by family" on the family thread, fired t9) | 0 | 0 / 0 | 9 |
| Crystal Lake t1-15, cost off (control) | 2 of 15 (t9 club destroyed, t14 an object stolen: the scene channel on its own) | 0 / 2 | none (channel off) | none | 0 | 0 | 0 | none | 0 |
| Crystal Lake t16-45, cost on | 26 of 30 (t16-18, 20-25, 27-35, 37-43, 45) | 24 by condition, 2 by item (t31, t32 the radio stolen) | 12 / 11 per 15-turn block (23 imposed; 4 the same text as a listed one) | not observable | 14 | 0 | 1 (t22 "cold, hard object") | 0 / 0 | 3 |

Baseline (round 10): 17 / 16 world-took; conditions 1/7/16 and 21/9; cleared 2 and 17; end 20 and 6; fatal: two deaths in the prose, both survived. Stamps: Harrowgate 30/30, Crystal Lake 28/30 (t19, t26 missing, the same turns the `worldClock` stamp is missing).

What the conditions are now: Harrowgate "discredited before the council", "lost the council's trust", "publicly undermined by family", "compromised credibility", "mother's public withdrawal of support", "irrevocably shattered council trust"; Crystal Lake "scraped face", "deepened gash on palm", "dislocated shoulder", "torn arm", "further wounded calf", "injured left hip". No feelings, no sensations, no sounds; the fan-out is gone (never more than one per turn, by construction). What is still wrong with them: (1) pressures filed as states in Harrowgate ("new weight of solitary expectation" t15, "increased pressure from failing efforts" t26 and again t28, "accelerated timeline pressure" t27); (2) re-imposition under new wording in both cells ("mother's palpable withdrawal of overt support" t17 and t18, "...public withdrawal of support" t19, "...withdrawal of overt backing" t21; "further shoulder and arm injury" t27 and t28; "further wounded calf" t29, t40, t42, t43) and re-imposition of the exact listed text (3 and 4 times), which the character store ignores on write but the count and the stamp record; (3) the count itself is the strike cadence: in Crystal Lake the fired section demanded an act on 17 of the 30 turns and nearly every act wounded the same character, so a condition a turn is the extractor reading the prose correctly. Worsening as CLEAR plus IMPOSE worked where it was tried (t22, t30, t33-35, t39, t45: 14 cleared, 3 left at the end against round 10's 6 from 30).

#1884, the terminal death: neither cell produced a death in 75 turns (round 10: two in 30), so the path was not exercised inside the cells. Exercised once, live, in a scratch session on the Crystal Lake world after the cells (artifact `fatal-test-crystal-lake-r11.json`): a custom action walking into the lake with the anchor chain produced a cliffhanger ("ripped you deeper into the unseen depths"), the extractor imposed "drowning" as a condition with `fatal: false` (a miss on "dying is never a condition", on prose that did not plainly kill); a second custom action stating the death plainly produced a segment the extractor read as `fatal: true` (and it cleared "drowning"), the segment was tagged `fatal-outcome`, the controller fired the fatal ending, the tragic epilogue generated and the ending screen rendered with no choices. The chain works end to end in the live app; its trigger is the extractor's read of a plain death, and a cliffhanger is not one.

### Gates, round 11
- G-F: PASS. 15 and 26 against a target of 3; the flag-off arm at its baseline shape (2 of 15 from the scene channel).
- G-G: FAIL in both cells on the count (2/9/4 against 3; 12/11 against 4). The Memory / Voice / Choice half holds against round 10 (Harrowgate 4/1/1, 3/1/1, 4/1/3 vs 3/2/2, 2/2/2, 2/2/3; Crystal Lake 4/2/1, 3/1/1, 3/2/2 vs 4/2/1, 3/2/2, 3/2/2: nothing more than 1 below). The count's composition changed: round 10's was feelings and fan-out; round 11's is re-imposition (exact and reworded) plus pressures in Harrowgate, and the strike cadence in Crystal Lake.
- G-H: PASS on the counts (0 and 1 unmatched items); no death reversed (none occurred); the fatal path holds live in the scratch test; one "drowning" condition on a cliffhanger turn in that test is named as the miss to watch.
- G1-G3, G6, G7: input contract unchanged at the seam (the extraction result gains `fatal`, the note gains `fatal`, both optional on the way out); leakage as declared (the cost section shows the character's own conditions and thread ids as before); determinism: scene JSON unchanged, extraction JSON gains one optional boolean, fail-open held (two missing stamps are missing JSON blocks); sizes above; zero new calls. Quality gate on the build: jest 435 / 3,041 exit 0, tsc 0, `npm run lint` 0 errors.

### Verdict, round 11
- HOLD on (c), by the declared rule: G-F passes in both cells, G-G fails on the count in both. The spam the round-10 memo named is gone (no feelings, no fan-out, worsening as clear plus impose), and what remains is two things: the extractor re-imposes a listed condition under new wording or the exact text, and in a cell where the fuse lands a strike on 17 of 30 turns the world takes something nearly every turn, which the count reads as spam and the prose reads as a siege.
- (b) holds: a death the app can read ends the session, proven live; nothing in the cells contradicts it because nothing in the cells died.
- What flips (c), bounded: (c2) `applyWorldCost` skips an imposed condition the character already carries, exact and case-insensitive, and does not record it on the note (today the store ignores the write but the stamp and the count keep it; 3 and 4 of this round's counts are that); the prompt shows the list it already shows. Re-imposition under new wording stays a prompt matter and is measured again. The G-G count threshold stays as declared and is re-read next round against a fuse that has an exit (#1872 log, re-entry (e)): if the strike cadence falls, the count falls with it; if the count stays high with the cadence down, the extractor is still the problem.
- Flag default stays off; PR #1883 carries the condition rule, the fatal field and the ending path and stays open. Ship/hold memo: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1882-world-cost.md`. Round posted to #1818 as round 11.

## Round 12, declared before the code (2026-08-20): the store refuses an exact re-imposition

The round-11 verdict's (c2), run in the same round as #1872's (a2), (e) and (f) on PR #1878 (its log carries the round-12 declaration for those and the matrix; comparator round 11, same cells, same autopilot, same judge prompts verbatim).

(c2) `applyWorldCost` skips an imposed condition the character already carries, compared exact after trim, case-insensitive. Skipped means skipped entirely: no store write (today the store ignores it anyway), no thread attribution, no entry on the note, so neither the stamp nor the count ever sees it. Round 11's counts carried 3 (Harrowgate) and 4 (Crystal Lake) exact-text re-impositions; this removes that slice deterministically. Re-imposition under new wording stays a prompt matter (the rule already forbids it) and is measured again. Nothing else moves on this PR: the condition rule, the parser cap, the fatal field and the ending path are round 11's, byte for byte.

The numbers: the round-11 table's columns, plus exact re-impositions skipped by the store per session (now observable as the gap between what the extraction imposed and what the note carries, read off the fetch tap where captured). Gates G-F, G-G, G-H unchanged, with G-G's count now read against a fuse that has an exit (#1872's (e)): the round-11 verdict said if the strike cadence falls and the count falls with it, the cadence was the count; if the count stays high with the cadence down, the extractor is still the problem and the next fix is named here.

Decision rule: unchanged from round 11. G-F, G-G and G-H hold in both cells -> SHIP (with the #1872 log's G-B and leak reads also holding: both PRs merge, `WORLD_COST` flips on). G-G fails on the count with the fired-section cadence visibly down -> HOLD on (c) naming the extractor; G-G fails with the cadence still high -> the read belongs to #1872's (e) and this PR holds with it. G-H unchanged.

## Round 12 results (2026-08-20, build `2dbb6053`; Crystal Lake attempt 2 on `deddf951`)

Built as declared: `applyWorldCost` reads the character's conditions at entry (trim, case-insensitive) and skips an imposed condition already carried - no store write, no thread attribution, no note entry. Tests: skip on exact-after-trim re-imposition (note, store and thread all untouched), reworded imposition still lands. One more change rides this PR from the round's deviation (declared in the #1872 log, "Mid-round deviation"): round 11's fatal-tag subscription re-fired on a remount that briefly renders with a fatal-ended session's id and the generated ending landed on the successor session (Crystal Lake attempt 2 was ended at turn 1 by it); fixed as `deddf951` - the subscription skips a session `endedSessions` already holds, test proven red without the fix. The matrix, judges and deviations are in the #1872 log; this section carries the cost numbers.

### The numbers that decide it, round 12

| Arm | World-took turns | Of which conditions / stolen-destroyed items | Conditions imposed per block | Exact re-impositions on the note | Cleared | Attributed to a thread | `item` costs with no matching `itemsLost` | `fatal` reads / deaths in the prose | Conditions at the end |
|---|---|---|---|---|---|---|---|---|---|
| Harrowgate, fresh 30, cost on | 14 of 30 | 14 by condition, 0 by items | 1 / 5 / 8 per 10-turn block | 0 (round 11: 3) | 0 | 0 | 0 | 0 / 0 | 14 |
| Crystal Lake attempt 1, t1-15 cost off (control) | 3 of 15 (t10, t11 bench leg stolen, t13 flannel destroyed: the scene channel) | 0 / 3 | none (channel off) | none | 0 | 0 | 0 | none | 0 |
| Crystal Lake attempt 1, t16-32 cost on (FATAL at t32) | 8 of 17 | 6 by condition, 2 by item (t20/21 the journal, t23/32 the multi-tool) | 4 / 1 per 15-turn block (round 11: 12 / 11) | 0 (round 11: 4) | 1 | 1 (t26 "sliced right bicep") | 1 (t32 "something small and hard from belt" - the multi-tool under a description) | 1 / 1, terminal | 4, all wounds |
| Crystal Lake attempt 2, t1-8 cost off (ended t8) | 0 observed | - | none (channel off) | none | 0 | 0 | 0 | none | 0 |

What the conditions are now: Crystal Lake's are right ("jagged splinter in right forearm", "sliced right bicep", "gash above hip" - lasting states, one per turn, a worsening cleared once). Harrowgate's count is the whole remaining problem and its composition is now clean of exact repeats: (1) pressures and outcomes filed as states ("request for documentation denied", "summoned to Albright's office on her terms", "Failed to gain clarity on bylaws"); (2) reworded re-imposition ("compromised ability to influence preliminary review" then "compromised influence"; "removed from... committee" then "removal from committee is public record"); (3) zero clears in 30 turns, so the list only grows (14 at the end against round 11's 9). The strike cadence is NOT the count anymore: the verdict's round-11 question is answered - the cadence fell in Crystal Lake (fired demands no longer wound every turn; 4/1 per block) and the count fell with it there, while Harrowgate's count stayed high with no cadence at all (its strikes were civic and rare), so the extractor's filing discipline is the residue, not the fuse.

#1884, in-cell for the first time: t32's prose (pinned, a blow to the already-injured temple, dragged into the undergrowth) read `fatal: true`, the tag stamped, the tragic epilogue auto-generated, the ending screen rendered with no choices, and the blind judge - who saw only the transcript - called the death "earned by what preceded it" while noting the eulogy embellished two facts. Four open threads resolved in the same reconcile with honest outcomes. No reversal. The remount defect this exposed (above) is the one thing the chain still owed and it is fixed and tested.

### Gates, round 12
- G-F: PASS in both cells (14 and 8 against a target of 3; the flag-off arms at their baseline shape, 3/15 and 0/8 from the scene channel).
- G-G: FAIL in Harrowgate on the count (1/5/8 against 3), PASS in Crystal Lake (4/1 against 4, the first pass in three rounds). The Memory/Voice/Choice half holds in both (nothing more than 1 below round 11's same block; Harrowgate block 2 is UP across the board).
- G-H: PASS. One unmatched item cost (at the cap of 1, and it is a naming mismatch, not an invention); the one fatal ended the session with no reversal and the judge read the death as earned; no false fatal in a cell (the attempt-2 turn-1 ending was the remount defect, caught, fixed, and that attempt discarded).
- G1-G3, G6, G7: input contract unchanged at the seam; determinism: the parser's behavior is round 11's, the store-side skip is deterministic; sizes unchanged (the WORLD COST section is byte-identical to round 11); zero new calls. Quality gate: #1872 log's round-12 gate run.
- Watch item, named: the treated arm lost 9 of 17 extraction stamps in Crystal Lake (the call ran with all sections aboard; no JSON came back). Round 11 lost 2 of 30. If round 13 sees the same rate, the extraction response has outgrown something and needs its own diagnosis before any more prompt freight is added.

### Verdict, round 12
- HOLD on (c) residue, by the declared rule: G-F and G-H pass in both cells, G-G splits - Crystal Lake passes with the cadence down (the round-11 verdict's "if the count falls with the cadence, the cadence was the count" branch, confirmed), Harrowgate fails on filing discipline (pressures-as-states, reworded re-imposition, zero clears). (c2) itself did exactly what it was built for: exact re-imposition is eliminated deterministically in both cells.
- What flips the residue is a prompt matter on the condition rule's letter (a pressure or a denied request is not a state of the character; an outcome already listed under other words is not a new cost; a state that no longer holds gets a CLEAR) - but the #1872 log's round-12 verdict says the next move on this defect family is not another wording pass while the leak/re-emission family is unsolved, and this PR holds with that read rather than shipping half.
- Flag default stays off; PR #1883 carries the condition rule, (c2), the fatal chain and the ended-session guard, and stays open. Ship/hold memo: `.claude/skills/narraitor-feature-experiment-lifecycle/memos/1882-world-cost.md`. Round posted to #1818 as round 12.

## Round 13, declared before the run (2026-08-25): measure the develop build that would ship

Round 13 is a measurement round. Nothing in the prompt changes. The question is whether the channel holds G-F, G-G and G-H on current `origin/develop` (`2861285c`) with `WORLD_COST` as the only experimental flag, which is the build that would actually ship if the default flips.

### Deviations from rounds 10-12

1. **No fuse.** Rounds 10-12 stacked PR #1878's thread deadlines onto develop. That draft is held and is not on this build: `WorldThread` has `dueByTurn` but no `firedAtTurn` or `strikeCount`, and the prompt has no `IN THE SCENE AND DUE NOW` section. Round 12 established that the strike cadence was Crystal Lake's G-G count. With that pressure absent, a Crystal Lake G-G pass is weaker evidence than round 12's pass.
2. **Base drift.** Eighteen prompt-path commits landed on develop since round 12's `2dbb6053` build, including #1899, #1896, #1934, #1929, #1922, #1920, #1918, #1916, #1909 and #1902. The Memory / Voice / Choice comparison to round 12 is confounded by this drift and by the missing fuse.
3. **Harrowgate gains a control arm.** Both cells use the same shape in one session: 15 turns with `WORLD_COST` off, then 30 turns with it on. This separates base drift from the flag in the cell that failed G-G in rounds 10-12, while shifting Harrowgate's treated block from t1-30 to t16-45.

One second attempt is budgeted per cell. If both attempts end inside the declared window, both attempts are reported and the last-block gate is unscorable. That is a finding rather than a harness failure.

### Matrix

Both cells use the same Cautious autopilot as rounds 6-12. The flag is build-time, so phase 2 restarts the dev server with `NEXT_PUBLIC_FEATURE_WORLD_COST=true` and resumes the same session through `/play` without `?fresh=true`. `WORLD_CLOCK` stays on in both phases as the shipped base.

| Cell | Phase 1, control | Phase 2, treatment |
|---|---|---|
| Harrowgate Mills, civic drama with no violence | t1-15, `WORLD_COST` off | t16-45, `WORLD_COST` on |
| Camp Crystal Lake, slasher horror | t1-15, `WORLD_COST` off | t16-45, `WORLD_COST` on |

Each cell becomes one transcript containing all four 10-turn blocks implied by the 15-off / 30-on shape. One blind judge scores each transcript against `narraitor-playtest-loop/rubric.md`. The judge receives only the transcript path and never sees the ledger.

### Metrics and gates

The primary metric remains **world-took turns per session**: a turn where an `itemsLost` entry carries `lossReason` `stolen` or `destroyed`, or a condition was imposed. The baseline is 0.

Recorded per arm: world-took turns; the conditions versus stolen or destroyed item split; conditions imposed per 10-turn block; exact re-impositions reaching the note; cleared conditions; thread attribution; `item` costs with no matching `itemsLost`; `fatal` reads versus deaths in the prose; and conditions at the end. The round-12 watch item is also explicit: extraction stamps present / treated turns. Round 12 lost 9 of 17 Crystal Lake stamps to the extraction output limit fixed by #1887 / PR #1899. If the rate remains bad, that diagnosis outranks the gates.

- **G-F - world-took turns:** at least 3 per session with the flag on, in both cells. Either cell below 3 is a failure.
- **G-G - no cost spam:** conditions imposed per 10-turn block at or below 3, and the rubric's Memory / Voice / Choice no more than 1 below round 12's same block.
- **G-H - consistency:** no more than 1 `item` cost without matching `itemsLost` per session; no death reversed on the next turn; no false fatal; and any condition contradicted by the prose within 3 turns named as a failure mode. A `fatal` that does not end the session or a kill in the prose with no `fatal` fails the death gate.

Decision rule: G-F, G-G and G-H hold in both cells -> SHIP (the `WORLD_COST` default flips to `true`). G-G fails on the count -> HOLD, naming which cell and which shape (pressures-as-states, reworded re-imposition, missing clears). G-H fails on a death -> HOLD on the fatal chain. G-F fails -> HOLD and the memo names whether the extractor is not recording takes the prose asserts, or the prose is not taking.

On a HOLD, the PR is record-only: this eval log, the memo, the artifacts pointer and the #1818 comment. No prompt wording ships unmeasured, and the flag stays off.

## Round 13 results (2026-08-25, build `2861285c`; declaration `a6f236e0c`)

Run as declared with no prompt wording changes and `WORLD_COST` as the only experimental flag. Both cells survived the full 15-turn control and 30-turn treatment windows, so neither used its budgeted second attempt. The first treated request in each cell carried both `WHAT THE WORLD CAN TAKE` and `WORLD COST`; all 30 treated scene calls per cell carried the take section, all 30 treated extraction calls carried the cost section, and the fuse-negative markers stayed absent (`fired` 0, `CONCLUDE` 0, `quiet` 0).

One harness-only session was discarded before Crystal Lake treatment: navigating from Harrowgate briefly auto-initialized a one-segment session with the Crystal Lake URL but Harrowgate's active character. No measured choice was played on it. The harness resumed the already captured Crystal Lake saved session by exact id, then verified its Crystal Lake world id, character id and 15-segment count before turn 16.

### The numbers that decide it, round 13

| Arm | Extraction stamps | World-took turns | Of which conditions / stolen-destroyed items | Conditions imposed per treated 10-turn block | Exact re-impositions on the note | Cleared | Attributed to a thread | `item` costs with no matching `itemsLost` | `fatal` reads / character deaths in prose | Conditions at arm end |
|---|---:|---:|---|---|---:|---:|---:|---:|---|---|
| Harrowgate, t1-15 control | 0/15, expected off | 0 of 15 | 0 / 0 | none, channel off | none | 0 | 0 | 0 | 0 / 0 | none |
| Harrowgate, t16-45 treatment | 30/30 | 2 of 30, t16 and t21 | 2 / 0 | 2 / 0 / 0 | 0 | 1 | 0 | 0 | 0 / 0 | `discredited before the council` |
| Crystal Lake, t1-15 control | 0/15, expected off | 0 of 15 | 0 / 0 | none, channel off | none | 0 | 0 | 0 | 0 / 0 | none |
| Crystal Lake, t16-45 treatment | 30/30 | 2 of 30, t36 and t43 | 2 / 0 | 0 / 0 / 2 | 0 | 0 | 0 | 0 | 0 / 0 | `bruised from fall`; `scraped knee` |

The round-12 stamp-loss watch item is closed on this build: 60 of 60 treated segments have `metadata.worldCost`, including 30 of 30 at Crystal Lake after round 12 returned only 8 of 17. The channel was live and the extractor recorded the four concrete lasting states the prose actually put on the player. No stolen or destroyed player item appeared in either treatment arm. Harrowgate repeatedly delivered, gifted or sacrificed the same laminated bylaws, but those reasons are deliberately outside the primary metric and no `item` cost was asserted for them.

The blind-judge Memory / Voice / Choice scores were:

| Cell | Control t1-15 | Treatment t16-25 | Treatment t26-35 | Treatment t36-45 |
|---|---|---|---|---|
| Harrowgate | 4 / 2 / 2 | 1 / 1 / 1 | 1 / 1 / 1 | 1 / 1 / 2 |
| Crystal Lake | 4 / 3 / 4 | 2 / 1 / 2 | 1 / 2 / 3 | 1 / 1 / 2 |

Against Harrowgate round 12's cost-on blocks (4/2/3, 2/3/3, 1/2/2), the first treatment block loses 3 Memory points and 2 Choice points, and the second loses 2 Voice points and 2 Choice points. That fails the rubric half of G-G even though the condition counts are under the cap. The judge's sharpest diagnosis is the missing durable procedural state: tabled motions, recess status, seconds and known bylaws reset so the Davies-Miller obstruction loop can replay. Crystal Lake's control is at or above its round-12 comparator (4/3/3), and its first treatment block is not below the scored round-12 treated block (1/1/2). Round 12 ended fatally at t32 and did not score a later block, so the remaining Crystal Lake same-block comparison has no scored comparator; its round-13 continuity still collapses around Chris's location, the arriving children, the bus departure and the mess-hall barricade.

### Gates, round 13

- **G-F: FAIL in both cells.** Harrowgate and Crystal Lake each recorded 2 world-took turns against the threshold of 3. This is not an extraction miss: the cost request was present on every treated turn, every treated turn returned a stamp, and each concrete lasting player state in the prose was recorded. The prose did not take from the player often enough. Harrowgate barely moved beyond a procedural loop; Crystal Lake moved aggressively around the player but spent most of that motion on off-screen victims and nearby threats rather than the player's inventory or status.
- **G-G: FAIL in Harrowgate; count PASS in both.** Conditions stayed at 2/0/0 and 0/0/2, exact re-impositions stayed at zero, and Harrowgate cleared one temporary condition. Harrowgate fails the rubric half on Memory, Voice and Choice drops greater than one in the first two treatment blocks. Crystal Lake's available scored comparator holds, but later round-12 blocks are unavailable because that run ended at t32.
- **G-H: PASS.** Both cells have zero unmatched `item` costs, zero false fatals, zero character deaths in the prose and no reversal. Harrowgate cleared `loss of momentum` at t18 and did not contradict `discredited before the council`; Crystal Lake carried the t36 bruise and t43 scraped knee through the following prose. The deaths implied around Crystal Lake concern NPCs, not the player, so `fatal: false` is the correct character-cost read.

### Verdict, round 13

- **HOLD**, by the precommitted rule. G-F fails in both cells at 2 of 30, and Harrowgate independently fails G-G's rubric half. G-H passes and the extraction-budget fix holds at 60 of 60 stamps, so neither the extractor contract nor truncation is the next problem.
- The flag stays off. This PR is record-only: declaration, results, memo and artifact pointer. No prompt wording or feature default changes.
- Re-entry is a scene-side behavior question, not another extraction wording pass: the world must impose at least three durable player costs without breaking the procedural or spatial state that the blind judges found. The artifacts are stored under `~/.claude/projects/-Users-jackhaas-Projects-personal-narraitor/artifacts/1882-round13-*`.
- Quality gate on the record-only diff: Jest 450 suites / 3,137 tests, `npm run type-check` exit 0, and `npm run lint` exit 0 with 127 existing warnings in visual-test files. No CSS, visual or E2E surface changed.
