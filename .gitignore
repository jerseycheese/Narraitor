# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.js

# testing
/coverage

# playwright
# Ignore generated reports and test results
/playwright-report/
/playwright-html-report/
/playwright-test-results/
/test-results/
/data/
index.html
!public/design-system/index.html
.playwright-mcp

# Playwright downloaded artifacts (from CI failures)
playwright-html-report/
playwright-test-results/

# Stryker (mutation testing) generated output
/reports/mutation/
.stryker-tmp/

# Playwright trace files (generated during debugging)
/trace/
trace/

# Playwright browser binaries and cache
.playwright/

# Note: Keep these Playwright files in repo:
# - playwright.config.ts (configuration)
# - tests/**/*.spec.ts (test files)
# - tests/**/*-snapshots/*.png (baseline screenshots - Darwin only)

# next.js
/.next/
/out/
# Next.js build artifacts in worktrees
worktrees/**/.next/
**/.next/trace

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dev_server.log
nextjs.log
.dev-log.txt
.dev-pid
.next-dev.log

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# storybook
storybook-static
*storybook.log
public/storybook

# IDE
.idea
.vscode
*.swp
*.swo

# Environment
.env
.env.development
.env.test
.env.production
project-config.json

# Temp files
.temp
.tmp

# Roo
.roo/mcp.json

# Docs (private, untracked)
# Use public_docs/ for committed documentation
docs/

# Generated dependency diagrams (regenerate with npm run deps:diagram:all)
public_docs/architecture/*.mmd
public_docs/architecture/*.svg
public_docs/architecture/*.dot
public_docs/architecture/*.html
dependency-violations.html

# Claude Code (CLAUDE.md is committed canon; the rest stays local)
.claude/*
.claude-workflow/
.clauderc
CLAUDE.local.md

# Claude Code cloud (web sessions): commit only these reviewed, shareable files.
# Everything else under .claude/ (incl. .github_token, settings.local.json) stays ignored.
!.claude/settings.json
!/CLAUDE.md
!.claude/skills/
!.claude/agents/
# never commit macOS cruft, even within the re-included dirs above
.claude/**/.DS_Store
# trigger-eval output is a dated measurement of a nondeterministic router: it is
# stale the moment a skill description changes, so it stays local
.claude/skills/_trigger_results/

# Codex
.codex/
AGENTS.local.md
AGENTS.md

# Gemini CLI
.gemini/
GEMINI.md

# Shared agent skills (synced locally, not committed)
.agents/

# MCP local config (do not commit)
.mcp.json

# Personal paths (security)
**/Users/*/

# Worktrees
narraitor-worktrees/
worktrees/

# Serena (local tooling)
.serena/
build-output.log

\n# Local tool logs\n.bdg/
characters-debug.png

# impeccable (design tool): keep local state out of git, share the live config and the
# artifacts decisions were made from. Briefs say what each route is for; sketches and
# mockups are the pictures behind those decisions, including the rejected ones the
# briefs cite by filename, and each carries a .json sidecar holding the prompt that made
# it. None of it is reproducible on demand, since the layout behind each one came from a
# randomised roll. Critique snapshots stay local: they regenerate from the code.
#
# Mockups are big, so this shares the directory rather than auto-adding to it. A new
# mockup shows up untracked and only lands if someone names it in a commit.
.impeccable/*
!.impeccable/live/
.impeccable/live/*
!.impeccable/live/config.json
!.impeccable/surfaces/
!.impeccable/sketches/
!.impeccable/mocks/

# design-loop skill's per-worktree session log — local paper trail, not repo docs
design-loop-log.md

# narraitor-playtest-loop's running findings log — same deal, findings graduate to issues
playtest-log.md
