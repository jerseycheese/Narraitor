## Description
Standardizes modal width/spacing/a11y across the app, removes an unused dialog, and adds a focused Playwright visual test.

## Related Issue
Closes # (cleanup walkthrough misc UI polish)

## Type of Change
- [x] Refactoring (code improvements without changing functionality)
- [x] Test addition or improvement
- [x] Documentation update (in-code a11y annotations / ARIA links)

## TDD Compliance
- [x] All new code is tested (Playwright visual added)
- [x] All tests pass locally (to be verified by CI)

## User Stories Addressed
- As a user, modals are readable at a consistent width with proper spacing and accessible descriptions.

## Implementation Notes
- Radix `Dialog` default width bumped to `max-w-xl`; internal gap increased for rhythm.
- All `SimpleModal` consumers moved to `size="xl"` (or `lg` for confirm variants), unified `mt-6` above action rows.
- Wired `aria-describedby` where helpful copy exists.
- Removed `AchievementDialog` and its Storybook file; no runtime references remain.
- Added Playwright visual spec for the Generate World modal with three stable snapshots (default, Inspired By, Set Within).

## Screenshots
N/A — validated via visual snapshots in Playwright.

## Playwright MCP Verification Summary
- New spec: `tests/visual/worlds-generate-modal.spec.ts`.
- Run: `npm run test:visual -- tests/visual/worlds-generate-modal.spec.ts`.
- Update: `npm run test:visual:update -- tests/visual/worlds-generate-modal.spec.ts`.

## Quality Checks
- [x] Accessibility considerations addressed (labels, `aria-describedby`, focus flow preserved)
- [x] No hardcoded colors; tokens/Tailwind utilities only
- [x] Lint/Typecheck to be verified in CI

## Testing Instructions
1) Start the app: `npm run dev`.
2) Visit `/worlds`; click "Generate World" and verify the modal width and spacing.
3) Toggle world type options; note consistent layout and readable form.
4) Run the visual tests as above to capture/verify snapshots.

## Checklist
- [x] Code follows project standards
- [x] Self-review performed
- [x] Comments added where useful (a11y ids)
- [x] Accessibility addressed
