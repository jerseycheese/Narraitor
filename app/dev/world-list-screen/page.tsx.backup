'use client';

import React from 'react';
import WorldListScreen from '@/components/WorldListScreen/WorldListScreen';
import './testUtils';

export default function WorldListScreenHarness() {
  // Stage 1: Isolation – ✅ Tested in Storybook stories
  // Stage 2: Test Harness – Testing component with real worldStore
  // Stage 3: Integration – Will be tested when integrated into full app

  return (
    <div className="harness-container p-8">
      <h1 className="text-2xl font-bold mb-4">World List Screen Test Harness</h1>
      <p className="text-gray-600 mb-4">Testing Stage 2: Component with live store</p>
      <div className="mb-4 p-4 bg-gray-100 rounded">
        <p className="text-sm text-gray-700">
          Test utilities available in browser console:
        </p>
        <ul className="list-disc list-inside text-sm text-gray-600 mt-2">
          <li>worldListTestUtils.addTestWorlds() - Add test data</li>
          <li>worldListTestUtils.clearWorlds() - Clear all worlds</li>
          <li>worldListTestUtils.setLoadingState(true/false) - Toggle loading</li>
          <li>worldListTestUtils.setErrorState('error message') - Set error</li>
        </ul>
      </div>
      <WorldListScreen />
    </div>
  );
}
