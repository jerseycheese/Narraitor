/**
 * Next.js Configuration
 * Compatible with Tailwind CSS v4 and PostCSS
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any necessary configuration here
};

export default nextConfig;
