// src/lib/inventory/__tests__/categories.test.ts

import {
  STANDARD_CATEGORIES,
  isValidCategory,
  getCategoryMetadata,
  getAllCategories,
} from '../categories';
import type { StandardInventoryCategory } from '@/types/inventory.types';

describe('Standard Inventory Categories', () => {
  // All tests removed - they were testing hardcoded constants and metadata structure,
  // which are implementation details rather than user-facing behavior
});
