import {
  templates,
  westernTemplate,
  sitcomTemplate,
  fantasyTemplate
} from '../worldTemplates';

describe('World Templates', () => {
  // All tests removed - they were testing hardcoded template structures (array lengths, lookups),
  // which are implementation details rather than user-facing behavior
});
