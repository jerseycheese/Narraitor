// src/types/npc.types.ts

import { EntityID, NamedEntity, TimestampedEntity } from './common.types';

/**
 * Represents a Non-Player Character (NPC) in the game world
 */
export interface NPC extends NamedEntity, TimestampedEntity {
  worldId: EntityID;
  avatarUrl?: string;
}
